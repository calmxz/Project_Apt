# Frontend env. Copy to .env.local and fill in values.
VITE_API_BASE_URL=http://localhost:8000/api

# Streaming chat (SSE). Default true. Set to "false" to fall back to the JSON path.
VITE_CHAT_STREAM=true
