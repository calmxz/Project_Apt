# QA re-triage Wave 4: Low (issue #326)

Branch `fix/wave-4-low` from `dev` (3c8b717). One PR, `Closes #326`.
Source of evidence: `docs/planning/2026-09-19-qa-retriage.md` verdict table (file:line per item, copied
into each task below so executors do not need the doc). Line numbers are from 2026-09-19 and may have
drifted slightly after Waves 1-3; grep for the quoted code, do not trust the number blindly.

## Triage (issue asks: fix cheaply in batch, or close wontfix with reason)

| Item | Verdict | Task |
|---|---|---|
| A-01 | fix (docs only) | E |
| B-08 | fix (docs + lower blueprint soft cap only) | E |
| C-14 | fix | A |
| C-15 | fix | B |
| C-16 | fix | B |
| C-17 | fix | A |
| C-18 | fix | A |
| D-21 | fix | C |
| D-22 | fix | D |
| D-25 | fix (delete composer Skip, with E-20) | C |
| E-15 | fix | C |
| E-17 | fix | C |
| E-18 | fix | D |
| E-19 | fix | D |
| E-20 | fix (delete `locked`) | C |
| F-20 | fix | D |
| F-21 | fix (client_id, not message_id) | C |
| G-13 | **wontfix** | E records reason |
| G-14 | fix | A |

G-13 wontfix reason: `run_streaming` is a ~494-line generator with metering, partial-persist and
tool dispatch interleaved across `yield` points. Extracting three seams is a behaviour-neutral
refactor with a large regression surface (streaming order, abort persistence, cost double-count
guard at `tutor.py:591-600`) and only a readability payoff. Revisit when a feature next touches
`run_streaming`; do it then under that feature's tests.

## Ground rules for every task

- Do not run git commands; the orchestrator commits per task. Stay inside your file scope; if you
  need a file another task owns, stop and report instead of editing it.
- Backend: run pytest from `backend/` (never repo root). After any import-touching change run the
  FULL backend suite. Frontend: `npm run test:unit -- --run <paths>` for your files, then
  `npx eslint <files>` and `npx prettier --check <files>` from `frontend/`.
- TDD: failing test first where the change is behavioural. Extend existing tests in the adjacent
  `__tests__/` or `backend/tests/` file before adding new files.
- `backend/contracts/` is codegen output. Edit `docs/api/openapi.yaml`, then run
  `python backend/scripts/gen_contracts.py` from repo root. CI enforces zero drift.
- Deleting a `data-testid` or prop: grep the whole repo including `frontend/e2e/` (vitest is not e2e).
- No emojis. ASCII only in code and console output. Copy: short, plain, no exclamation marks.
- Report: exact file list, test counts before/after, anything you could not do and why.

## Task A (opus) - backend routes, errors, insights split

Files: `backend/main.py`, new `backend/lib/error_handlers.py` (name free), `backend/routes/sessions.py`,
`backend/routes/chat.py`, `backend/routes/profile.py` (import line only), `docs/api/openapi.yaml`,
`backend/contracts/models.py` (via codegen only), `backend/services/profile_service.py`,
new `backend/services/profile_insights.py`, `backend/tests/test_profile_aggregate.py`, new/extended
tests. Do NOT touch `backend/db/`.

- **C-14** Evidence: repo-wide grep for `exception_handler` returns zero hits; `main.py:56-70`
  registers only BodySizeLimit + CORS + RequestId middleware. `X-Request-Id` is emitted by
  `backend/lib/request_id.py:22-30`. Contract: `ErrorResponse.detail: str | CodedErrorDetail`,
  `CodedErrorDetail` = `{code: str, **extra}` (`contracts/models.py:748-760`).
  Fix: (1) a catch-all for unhandled exceptions returning 500 with body
  `{"detail": {"code": "internal_error", "message": "Something went wrong.", "request_id": <id>}}`,
  logged with traceback. **Layering trap:** `app.add_exception_handler(Exception, ...)` runs in
  Starlette's ServerErrorMiddleware, which is OUTSIDE `CORSMiddleware`, so a browser gets an opaque
  CORS failure instead of the body (the C-03 lesson in the `main.py` comment). Implement the catch-all
  as an ASGI/BaseHTTP middleware registered with `add_middleware` BEFORE the CORS line (first
  registered = innermost), or otherwise prove in a test that a cross-origin 500 carries
  `access-control-allow-origin`. Read `lib/request_id.py` for where the id lives. (2)
  `app.add_exception_handler(ValueError, ...)` -> 422 with `{"code": "invalid_value", "message": str(e),
  "request_id": <id>}`; this one may use the normal handler (ExceptionMiddleware is inside CORS).
  Keep the existing route-level catches (`routes/profile.py:112` -> 422 `str(e)`, `routes/upload.py:131`)
  untouched. Tests: one for each path; the 500 test needs
  `TestClient(app, raise_server_exceptions=False)`. Also assert the 500 body has `X-Request-Id`
  header equal to the body `request_id`.
- **C-17** Evidence: `backend/routes/sessions.py:287` `q: str | None = None` with no
  `Query(..., max_length=...)`; `:300` `base.where(SessionModel.topic.ilike(f"%{q}%"))` with no
  escaping and no `escape=`. Spec `docs/api/openapi.yaml:151-154` declares `q` with no `maxLength`,
  unlike the capped `topic` param on `/sessions/lookup`. Fix: `Query(None, max_length=200)`; add
  `maxLength: 200` to `q` in the YAML; run codegen; escape `\`, `%`, `_` in `q` (backslash first)
  and pass `escape="\\"` to `ilike`. Tests: 201-char `q` -> 422; `q="50%"` matches only a topic
  containing a literal `50%`; `q="a_b"` does not match `axb`.
- **C-18** Evidence: `backend/routes/chat.py:224` `.order_by(ChatMessage.created_at.desc())` and
  `backend/routes/sessions.py:659` same, no secondary key, while `services/session_enrichment.py:78`
  and `sessions.py:307` already use `(created_at.desc(), id.desc())`. Fix: append
  `ChatMessage.id.desc()` to both. Test: two messages with identical `created_at` come back in
  id-desc order.
- **G-14** Evidence: `backend/services/profile_service.py` is 695 lines; the cross-session insights
  half is `_monday` (:506), `_learning_insights` (:510), `aggregate_for_user` (:572) plus whatever
  private helpers only they use. No `services/profile_insights.py` exists. Callers:
  `routes/profile.py:26` and `tests/test_profile_aggregate.py:373,400,412`. Fix: move that half into
  `backend/services/profile_insights.py`; it may import `canon`, `concept_names`, etc. FROM
  `profile_service`; `profile_service` must not import `profile_insights` (no cycle, no re-export
  shim). Update both callers. Run the FULL backend suite afterwards (import refactor).

## Task B (opus) - schema constraints migration

Files: `backend/db/models.py`, new `backend/db/alembic/versions/0028_<slug>.py`, tests under
`backend/tests/` for models/migrations only. Nothing else. Latest revision is `0027_chat_messages_sid_id_desc`.

- **C-15** Evidence: `backend/db/models.py:145` `status: Mapped[str] = mapped_column(String,
  default="pending")` no CheckConstraint; `:88` `role: Mapped[str] = mapped_column(String,
  nullable=False)` no CheckConstraint; `Document.__table_args__` (:136-138) holds only an index;
  `chat_messages.status` DOES get a CHECK at `:79-82` (copy that pattern). Allowed sets: derive
  `documents.status` from the `DocumentStatus` enum in `backend/contracts/models.py` (~:552;
  `processing` is legitimate now) and confirm every writer of `Document.status` in
  `backend/services/` uses only those values; derive `chat_messages.role` by grepping every writer
  of `ChatMessage(role=` / `.role =` (do not assume `user`/`assistant` only).
- **C-16** Evidence: `0001_phase7_baseline.py:27,36,47,63,73` all five `created_at` columns
  `nullable=True`; no later migration alters them; `models.py:21,62,92,125,151,173` have Python-side
  `default=_utcnow` only, no `server_default`. Fix in models: `server_default=func.now()`,
  `nullable=False`, keep `default=_utcnow`.
- **Migration 0028** (one file, both items): `SET lock_timeout = '5s'` at the top (C-12 rule);
  for each of the five tables `UPDATE ... SET created_at = now() WHERE created_at IS NULL`, then
  `ALTER COLUMN created_at SET DEFAULT now()`, then `SET NOT NULL`; the two CHECK constraints via
  `ADD CONSTRAINT ... CHECK (...) NOT VALID` then `VALIDATE CONSTRAINT`. **Dialect trap:** CI runs the
  migration chain on sqlite; `NOT VALID`/`VALIDATE` and `ALTER COLUMN` are Postgres-only. Branch on
  `op.get_bind().dialect.name`: sqlite path uses `op.batch_alter_table` (or a no-op for the CHECK
  where batch mode cannot express it, with a comment) and Postgres path uses the raw statements.
  Look at how earlier migrations (0019-0027) handle sqlite vs postgres and copy that idiom.
  Downgrade must drop both constraints and restore nullable. Write a test that upgrades head on
  sqlite and asserts `documents.status = 'bogus'` is rejected via the model CHECK.
- Report the exact allowed value sets you chose; the orchestrator runs a live precheck
  (`SELECT DISTINCT status FROM documents`, `SELECT DISTINCT role FROM chat_messages`, NULL counts
  for `created_at`) against Supabase before merge.

## Task C (opus) - session view, composer, check card, optimistic row

Files: `frontend/src/views/SessionView.vue`, `frontend/src/stores/session.js`,
`frontend/src/components/chat/Composer.vue`, `frontend/src/components/chat/CheckQuestion.vue`,
`frontend/src/components/chat/MessageList.vue`, their tests, `frontend/e2e/` only to delete
references to removed testids. Do NOT touch `SessionHeader.vue`, `ProfileView.vue`, `HomeView.vue`,
`useTheme.js`.

- **D-21** Evidence: `SessionView.vue` has no `h1` before a topic resolves; the only session-branch
  `h1` is `components/chat/SessionHeader.vue:22-23` gated `v-if="topic"`, and `SessionView.vue:452-455`
  `headerTopic` returns `''` until `current`/`knownRow` loads; `SessionView.vue:6`'s `h1` is the 404
  branch only. Fix: a visually-hidden fallback `<h1 class="sr-only">Session</h1>` rendered ONLY while
  `headerTopic` is empty and the 404 branch is not shown (never two `h1`s). Check a11y tests for
  single-`h1` assertions and extend them.
- **E-20 + D-25** Evidence: `stores/session.js:538` `const checkLocked = computed(() => false)`,
  returned at `:1015`, bound at `SessionView.vue:182 :locked="store.checkLocked"`; Composer branches
  `Composer.vue:17` (attach disabled), `:132-139` (Skip button), `:191` (prop), `:227` (placeholder),
  `.composer-skip` grid rule (~:452, shares `grid-column: 3` with send/stop, the D-25 overlap);
  `@skip` wiring at `SessionView.vue:180`. `CheckQuestion.vue:119-128` now owns Skip. Fix: delete
  all of it (prop, branches, CSS, store computed + export, view binding + handler, tests that assert
  the composer Skip). Grep `frontend/e2e/` for `composer-skip` / Skip testids.
- **E-15** Evidence: `SessionView.vue:925-928` (timed-out branch of `applyUploadOutcome`) writes
  `uploadStatus = { kind: 'pending', text: '<file> is still processing...' }` that nothing clears,
  while `ReferenceStatusBanner` already owns steady-state ingestion status and both stack at
  `:152-159`. Fix: on timeout set `uploadStatus.value = null` (banner owns the state from here); do
  not leave the earlier "Uploading..." chip stuck. Update the test that asserts the old copy.
- **E-17** Evidence: `CheckQuestion.vue:70-77` options gated only on `answered` (from `item.status`,
  `:17`), which flips after the POST returns; `busy` prop (`:10`) is bound to
  `store.streamState !== 'idle'` and drives only Skip/Next/Done; `session.js:567` is a silent
  `if (checkAnswering.value) return`; `checkAnswering` (`:539`) is NOT in the store return object.
  Fix: export `checkAnswering`; new prop `answering` on CheckQuestion bound from SessionView;
  options `:disabled="answered || answering"` with `aria-busy` on the group while answering. Leave
  `busy` semantics alone. Test: second click during answering does not emit.
- **F-21** Evidence: `session.js:807` pushes the optimistic user row as `{ role: 'user', content }`
  with no id; `:891` pops it on failure via `message_id === undefined`; `MessageList.vue:50` keys on
  `m.message_id || 'm-' + i`; `session.js:310` still prepends older pages. **Cursor trap:**
  `session.js:320` `_hasServerId` treats any non-null, non-`'pending'` `message_id` as a server id
  and `:344` uses the oldest such id as the pagination cursor, so a client UUID in `message_id`
  would corrupt paging. Fix: add `client_id` (`crypto.randomUUID?.() ?? fallback`) on the optimistic
  row; key MessageList on `m.message_id ?? m.client_id ?? 'm-' + i`; pop-on-failure matches by
  `client_id`. Only reconcile `message_id` from the stream if you verify the SSE actually emits the
  persisted USER message id (read `backend/routes/chat.py` / `backend/agent/tutor.py`); if it does
  not, leave the row keyed by `client_id` and say so in the report. Test: two rapid optimistic rows
  get distinct keys; failure pops the right one.

## Task D (sonnet) - small frontend fixes

Files: `frontend/src/components/chat/SessionHeader.vue`, `frontend/src/views/ProfileView.vue`,
`frontend/src/views/HomeView.vue`, `frontend/src/composables/useTheme.js`, their tests. Nothing else.

- **D-22** Evidence: `SessionHeader.vue:85-88` `.session-topic-link { text-decoration: none; cursor:
  pointer; }` styled identically to `.session-topic` (`:71-83`) at rest; only hover (`:90-95`) and
  `title` (`:28`) hint it is a link; `:26` is the only link to `/session/:id/profile`. Fix: resting
  affordance - `text-decoration: underline; text-underline-offset: 0.15em; text-decoration-color:
  <existing muted/learner token from the theme>`; keep hover. Do not change copy.
- **E-18** Evidence: `ProfileView.vue:400` `onMounted(load)` with no `watch(() => props.id, ...)`;
  `topicLabel` resolves from the store (`:327-330`). Fix: `watch(() => props.id, load)` plus a
  load-sequence counter so a slow earlier load cannot overwrite a newer one; copy the idiom from
  `SessionView.vue:645-656`. Test: id change triggers reload; stale response is discarded.
- **E-19** Evidence: `HomeView.vue:97-99` `startQuick` calls `begin(quickTopic.value)` with no await
  and no `.catch()`; `useStartFlow.js:15-38` `begin` has try/finally only and `_create` rethrows
  non-409 (`:87`). Sibling sites already use the deliberate empty catch (`SessionsLibraryView.vue:27`,
  `SidebarSessionRow.vue:65`). Fix: `begin(quickTopic.value).catch(() => {})` with a one-line comment
  that `useStartFlow` already surfaces the error. Test: rejected `begin` does not produce an unhandled
  rejection.
- **F-20** Evidence: `useTheme.js:41-57` `init()` adds the `matchMedia` change listener with no
  removal path, no `dispose()`, no re-entry guard; `handler` is a local closure. Fix: `if (mediaQuery)
  return` guard, hoist `handler` to module scope, export `dispose()` that removes the listener and
  nulls `mediaQuery`. Test: `init()` twice adds one listener; `dispose()` removes it.

## Task E (sonnet) - docs and blueprint cap

Files: `docs/decisions.md`, `docs/reference.md`, `render.yaml`, `README.md` (line ~234 only),
`docs/auth/supabase-setup.md` (lines ~82-83 only), `.env.example` (comment only). Nothing else.

- **A-01** Evidence: no revocation primitive exists (grep `token_valid_after|denylist|jti|revoke`
  over `backend/` = 0); `backend/services/auth.py` validates signature/exp only; the old
  `docs/security/SECURITY_REVIEW.md` home was deleted 2026-09-19 and no doc records the residual
  window. `docs/auth/supabase-setup.md:161-178` leaves the access-token expiry at the Supabase
  default (~1 hour) and sets refresh-token expiry to 30 days. Fix: in `docs/decisions.md` (new
  section `## 2026-09-21 - QA re-triage Wave 4 (issue #326): triage and deviations`) record the
  stateless-token tradeoff: logout/ban takes effect at the next access-token expiry; worst-case
  residual window = configured access-token TTL (state the value from the setup doc); refresh tokens
  ARE revoked server-side by Supabase on sign-out. Add a two-line pointer in `docs/reference.md`
  under Backend / auth.
- **B-08** Evidence: `render.yaml:34-37` pins `LLM_SOFT_CAP_USD=0.8`, `LLM_HARD_CAP_USD=1.00`;
  `backend/config.py:67-68` defaults `2.00`/`3.00`; `.env.example:44-45`, `docker-compose.prod.yml:39-40`,
  `README.md:234`, `docs/auth/supabase-setup.md:82-83` all say `2.00`/`3.00`; `cost_meter.py:172`
  derives urgent = `hard_cap * 0.9`, so the blueprint tiers are 0.80/0.90/1.00 - too tight to act on.
  Decision (orchestrator): the hard cap is untouched; lower ONLY the blueprint soft cap to `0.50`
  so tiers become 0.50/0.90/1.00; source of truth for the blueprint values is `render.yaml`, for
  local/dev defaults `backend/config.py`. Fix: edit `render.yaml`; add a "Cost cap tiers" table to
  `docs/reference.md` (soft / urgent=0.9*hard / hard; blueprint vs local default; env var names);
  amend the README and supabase-setup lines to say "local default; the Render blueprint pins lower
  values, see docs/reference.md"; add the same one-line comment in `.env.example`. Call them
  "blueprint values", not "deployed" (deploy is not confirmed live).
- **G-13 wontfix**: add the reason from the Triage section above to the same decisions.md section.
- **Triage table**: copy the Triage table verdicts into the decisions.md section in one sentence
  each (18 fixed in PR, 1 wontfix).

## Orchestrator (after all tasks commit)

- Live precheck via Supabase MCP for Task B's value sets and `created_at` NULL counts.
- `migration-reviewer` agent on 0028 before its commit.
- Full `pytest` from `backend/` (baseline ~1017 collected), full `npm run test:unit -- --run`
  (baseline 1208), `npm run lint`, codegen drift check (`git status` clean after
  `python backend/scripts/gen_contracts.py`).
- `reviewer-opus` x2 (backend A+B, frontend C+D). `/code-review` on the branch.
- PR to dev `Closes #326`; merge with a MERGE COMMIT; close #326 by hand (PRs to dev never
  auto-close). Owed after merge: `alembic upgrade head` on live to 0028, then verify.
