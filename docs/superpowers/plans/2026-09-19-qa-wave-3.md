# QA re-triage Wave 3: Medium frontend (issue #325 + #330)

Branch `fix/wave-3-medium-frontend` from `dev` (8d6d19a). One PR, `Closes #325`, `Closes #330`.
Source of evidence: `docs/planning/2026-09-19-qa-retriage.md` verdict table (file:line per item, copied
into each task below so executors do not need the doc).

## Ground rules for every task

- Frontend only. Do not touch `backend/`. Do not run git commands; the orchestrator commits per task.
- Read `frontend/src/**/__tests__/` for the file you edit first; extend existing tests, add new ones for
  new branches. TDD: failing test first where the change is behavioural.
- Verify with `npm run test:unit -- --run <paths>` for your files, then `npx eslint <files>` and
  `npx prettier --check <files>` from `frontend/`. Report exact file list + test counts.
- No emojis. ASCII only in code and console output.
- Copy is owned by us: short, plain, no exclamation marks. Match the voice in `lib/errors.js`.
- Confirm dialogs use the exact styling contract from `components/chat/ReferenceStatusBanner.vue:180-190`:
  `rejectLabel: 'Cancel'`, `rejectClass: 'p-button-text p-button-secondary'`,
  `acceptClass: 'p-button-danger confirm-delete-strong'`, no icon.

## Task A (opus) - store + stream + error codes

Files: `stores/session.js`, `services/chatStreamService.js`, `lib/errorCodes.js`, `lib/errors.js`,
`components/chat/CapBanners.vue` (only if needed), their tests.

- **#330** `errorCodes.js`: add `ERR_EMPTY_MESSAGE='empty_message'`, `ERR_EMPTY_TOPIC='empty_topic'`,
  `ERR_BODY_TOO_LARGE='body_too_large'`, `ERR_SESSION_ENDED='session_ended'`, `ERR_TOOL_FAILED='tool_failed'`.
  Backend sites: `backend/routes/chat.py`, `routes/sessions.py`, `routes/upload.py`, `lib/body_limit.py`
  (read them for the exact code strings, do not edit). Add `CODE_COPY` entries in `errors.js`.
- **E-04** `session.js:642` and `:858` both do `error.value = data.message || data.code` for SSE
  `error` events. Add an exported `sseErrorCopy(data)` in `errors.js` that maps `data.code` via
  `CODE_COPY` (fallback: `data.message`, then generic). Make `_applyCapError(detail)` return `true`
  when it claimed the event (a cap code); when it did, do NOT set `error.value` (CapBanners already
  renders the cap). Extract the shared `error` case into one helper so the two blocks cannot drift.
- **E-02** `chatStreamService.js:93-105` body-phase catch: `throw e instanceof TypeError ?
  new ApiError(0, { detail: e.message }, path) : e`, mirroring `:61`.
- **E-03** `session.js:906-909` generic transport catch: before `_clearStreamState()`, if
  `streamingMessage.value?.content` is non-empty, push it into `messages` with `status: 'error'`
  exactly as `handleAbortError` (`:775-784`) does. Factor the "retain partial" step into a helper both use.
- **F-16** cap retained history. Policy (decided): `MAX_RETAINED_MESSAGES = 200`, enforced on live
  append only (new user/assistant messages, finalize). When exceeded, drop from the top and set
  `hasMoreMessages.value = true` so the user can page back. Manual prepend (`:310`) is NOT capped.
  Verify the load-earlier cursor is derived from the oldest retained message id so eviction stays
  consistent; if it is not, make it so.
- **E-06 (store side)** `session.js:91,109`: `libraryError` is rendered nowhere; delete it and the export.
  `fetchLibrary` (`:103-113`) passes `{ silent: true }` to `getSessionLibrary` so the view owns the copy.
  Check `SessionsLibraryView.vue` still compiles (Task D edits that file; you only touch the store).

## Task B (opus) - markdown render + bubbles

Files: `components/chat/MarkdownContent.vue`, `lib/markdownRenderer.js`, `lib/markdownStreamBuffer.js`,
`components/chat/UserBubble.vue`, `components/chat/MessageList.vue`, their tests.

- **F-15** `MarkdownContent.vue:15-25`: `parts` re-renders the whole safe prefix every update. Cache the
  rendered HTML of the stable prefix: keep `{ prefixText, prefixHtml }` in `splitState`; when the new
  `safe` starts with the cached `prefixText` AND the cached prefix ends on a block boundary the splitter
  already produces, render only the new slice and concatenate; otherwise full render. Invalidate on
  `markdownAssetsVersion` change and when `streaming` flips false (final full render). Test: for
  several streaming fixtures (lists, fences, tables, math, headings split across chunks), incremental
  output must be byte-identical to `renderMarkdown(fullText)`; if any fixture differs, narrow the
  boundary rule until it is. Do not weaken DOMPurify.
- **D-13** add `overflow-wrap: anywhere; word-break: break-word` to `UserBubble.vue` `.content`
  (`:70-76`) and `MarkdownContent.vue` `.md-rendered` (`:169-178`). Wrap rendered `<table>` in a
  `<div class="md-table-wrap">` with `overflow-x: auto` (markdown-it `table_open`/`table_close` rule
  override in `markdownRenderer.js`, or a post-render pass; keep DOMPurify allowlist valid).
- **D-07** `MessageList.vue:68` `aria-label` on a `<p>`: replace with a visually-hidden `<span class="sr-only">`
  (check `assets/base.css` for the existing sr-only utility name and reuse it).

## Task C (opus) - a11y / layout / tokens

Files: `components/chat/Composer.vue`, `assets/base.css`, `assets/dialogs.css`, `components/GapPickerDialog.vue`,
`components/sidebar/Sidebar.vue`, `components/sidebar/SidebarSessionRow.vue`, `components/sidebar/SidebarRowMenu.vue`,
`views/SettingsView.vue`, `components/settings/UsageTab.vue`, `components/settings/ProfileTab.vue`,
`__tests__/hitTargets.test.js`, other tests for these files.

- **D-09** `Composer.vue:157-159`: char counter has `aria-live="polite"` and changes per keystroke.
  Keep the visible counter but move `aria-live` to a separate visually-hidden span that only renders text
  when `nearCharLimit` (>=90%) or at the limit ("N characters left" / "Character limit reached");
  give the textarea `aria-describedby` pointing at a static limit hint.
- **D-20** `Composer.vue:275` resting border `--card-edge` = 1.50:1 light / 1.19:1 dark on `--card`.
  Add `--control-edge` at all three declaration sites in `base.css` (`:137` light, `:215` dark, `:275`
  media dark) with >=3:1 against `--card` (light `#ffffff`: e.g. `#767c86` = 3.9:1; dark `#22252b`:
  pick and verify with a contrast calc in the report). Use it on the composer and any other text
  input/textarea resting edge in the files you own (search `var(--card-edge)` on inputs; leave card
  chrome on `--card-edge`). Record the token in `docs/reference.md` (orchestrator will merge; put your
  paragraph in the report).
- **D-12** `GapPickerDialog.vue:6` `width: '24rem'` -> `width: min(24rem, calc(100vw - 2rem))`; also
  add `max-width: calc(100vw - 2rem)` to `.crux-dialog` in `dialogs.css`.
- **D-16** `SettingsView.vue:20`: only the active panel is in the DOM (`:33-43`) so `aria-controls` on
  unselected tabs dangles. Bind `:aria-controls` only when `t.slug === tab` (keep `<KeepAlive>`;
  Task E-10 relies on it).
- **D-17** add `hit-44` (and `coarse-2x` where the row layout allows) to `Sidebar.vue:647-651`
  (drawer-footer Settings link), `:676-681` (Sign out button), `SidebarSessionRow.vue:265-271`
  `.sb-row-button`. Extend `__tests__/hitTargets.test.js` to cover the three.
- **D-18** `Sidebar.vue:713` `.sidebar { height: 100vh; }` -> add `height: 100dvh;` on the next line
  (keep `100vh` as fallback), matching `SessionView.vue:1039-1040`.
- **E-12** `SidebarSessionRow.vue:53-69` `onEnd` calls `store.endSession` directly. Route through
  PrimeVue `useConfirm` with the contract above: header `End session`, message
  `End "<title>"? You can still read it, but you cannot continue the conversation.`, accept `End session`.
  Keep the `busy` guard. Check `App.vue` mounts `<ConfirmDialog>` globally (it does for file delete).
- **E-10** `UsageTab.vue:26-34` and `ProfileTab.vue:110-122`: add `error` state with a Retry button
  bound to `load()`; add `onActivated(() => { if (error.value) load() })` (KeepAlive re-activates).

## Task D (opus) - views: review, library, profile, route focus, vercel

Files: `views/ReviewView.vue`, `views/SessionsLibraryView.vue`, `views/ProfileView.vue`, `router/index.js`,
`App.vue`, `components/auth/AuthCover.vue`, `views/LegalView.vue`, `views/NotFoundView.vue`,
`frontend/vercel.json`, `frontend/nginx.conf`, their tests.

- **D-11** `ReviewView.vue:108-123`: keep `{ silent: true }` and never-throw, but add `loading` and
  `error` refs distinct from emptiness. Render: loading -> skeleton (reuse an existing skeleton
  component if one fits, else a minimal 3-row placeholder); error -> "Could not load your review queue."
  + Retry button calling the loader; empty -> existing "Nothing due right now."
- **E-06 (view side)** `SessionsLibraryView.vue:279-281`: first-load failure renders a bare paragraph.
  Add a Retry control in that branch bound to the loader. The store now passes `{silent:true}`
  (Task A), so the page copy is the only surface; do not import `store.libraryError` (deleted).
- **E-08** `ProfileView.vue:348-364` `_applyWrite`: add a `writing` ref; while true, disable every
  mutating control (`:disabled="writing"`) and ignore re-entrant calls. Chain writes: each call reads
  the ETag from the latest `data.value` after the previous write resolves (serialise via a promise
  queue). Add `useConfirm` (contract above) to `removeItem` (`:176-179`) and `removeSubtopic`
  (`:124-127`): header `Remove concept`, accept `Remove`.
- **D-07** `ProfileView.vue:233` `aria-label` on a `<span>`: replace with visually-hidden text.
- **D-15** `router/index.js:185-192` `document.getElementById('main-content')?.focus()` silently misses
  on chrome-less routes (`App.vue:131-135` `v-else` RouterView; `AuthCover.vue:2`, `LegalView.vue:2`,
  `NotFoundView.vue:2` roots carry no id). Give both `App.vue` branches a stable focus target
  (`id="main-content"` + `tabindex="-1"` on the `v-else` wrapper or on those three view roots),
  and in the router `afterEach`: `const el = ...; if (!el) { if (import.meta.env.DEV)
  console.warn('[router] focus target #main-content not found for', to.path); return }`.
  Unit-test the warn path.
- **F-17** `vercel.json`: add a `headers` block `{ "source": "/assets/(.*)", "headers": [{ "key":
  "Cache-Control", "value": "public, max-age=31536000, immutable" }] }`. Align `nginx.conf:61-63` to
  `expires 1y; add_header Cache-Control "public, immutable";` (Vite hashes asset filenames).

## Task E (opus) - reference polling, api retry, auth copy

Files: `components/chat/ReferenceStatusBanner.vue`, `views/SessionView.vue` (only `pollUploadStatus`
and the upload flow around `:840-915`), `components/chat/UploadStatus.vue`, new
`composables/useReferencePoll.js`, `services/apiClient.js`, new `lib/authErrors.js`,
`views/LoginView.vue`, `views/RegisterView.vue`, `views/ForgotPasswordView.vue`,
`views/ResetPasswordView.vue`, `components/settings/AccountTab.vue`, their tests.

- **F-12 + E-07** (one design). Today two independent pollers: `SessionView.vue:881-908`
  `pollUploadStatus` (fixed 1s, 90 attempts) and `ReferenceStatusBanner.vue:155-166` (fixed 2s).
  Build `composables/useReferencePoll.js`: one poller per session id, exponential backoff
  2s -> 4s -> 8s -> cap 15s while any doc is pending, reset on success, `failed` flag set when a poll
  throws (including the FIRST poll, where `status` is still null), retry-with-backoff continues while
  failed, exposes `{ docs, status, failed, retry, watch(documentId, filename) }`. Banner renders a
  "References unavailable - Retry" row when `failed` (per-file delete buttons stay reachable when we
  have a last-known list). Delete `pollUploadStatus`; the chip in `SessionView` is driven from the
  composable's per-document transitions (`pending -> ready | failed`), preserving the current chip
  copy at `:888-912` including the "still processing" ceiling message (attempt ceiling becomes a time
  ceiling of ~90s). Keep the `gen` generation guard semantics (`:299`).
- **F-18 (frontend half)** `apiClient.js:111-166`: wrap `fetch` in bounded retry for GET only:
  attempts 3, delays 300ms/900ms (+/-20% jitter), on network error (`TypeError`/`TimeoutError`) and
  502/503/504 only. Fresh `AbortSignal.timeout` per attempt. `reportApiError` fires once, after the
  final failure. Must not interfere with the `_retried` 401 refresh path and must not touch
  `chatStreamService`. Add a short TTL GET cache (5s, keyed by url, in-memory Map, bypass with
  `{ fresh: true }`, invalidated on any non-GET to the same path prefix). Backend HTTP `ETag` is out of
  scope; the orchestrator opens a follow-up issue.
- **E-13** new `lib/authErrors.js`: `authErrorCopy(e, fallback)` keyed on Supabase `AuthError.code`
  (auth-js 2.112.3 emits `.code`, type `ErrorCode` in
  `node_modules/@supabase/auth-js/dist/module/lib/error-codes.d.ts`) then `.status`, never prose:
  `invalid_credentials`, `email_not_confirmed`, `user_already_exists`/`email_exists`, `weak_password`,
  `same_password`, `otp_expired`, `over_email_send_rate_limit`, `over_request_rate_limit`,
  `email_address_invalid`, `session_expired`; status 429 -> throttled copy, 5xx -> generic. Export
  `isEmailNotConfirmed(e)` = `e?.code === 'email_not_confirmed'`. Replace the five
  `error.value = e?.message || ...` sites (`LoginView.vue:168-170,182`, `RegisterView.vue:159`,
  `ForgotPasswordView.vue:102`, `ResetPasswordView.vue:137`, `AccountTab.vue:203`) and drive
  `needsConfirm` from `isEmailNotConfirmed`. Do NOT route through `friendlyError` (constraint from
  commit `1d0f4aa`, comment at `LoginView.vue:165-168`; update that comment to point at the helper).

## Orchestrator (after all tasks commit)

- `docs/reference.md`: `--control-edge` token, `useReferencePoll` backoff, apiClient GET retry/cache,
  `authErrors` helper, message cap.
- `docs/decisions.md`: F-18 frontend/backend split (follow-up issue), F-16 cap policy, D-16 choice.
- Full `npm run test:unit -- --run` (baseline 1004), `npm run lint`, `npm run format`.
- Whole-branch review in main loop. PR to dev with `Closes #325`, `Closes #330`; body lists owed
  browser checks: D-12 at 320px, D-13 long URL + wide table, D-18 mobile drawer, D-20 contrast, F-15
  streaming parity in the real app.
