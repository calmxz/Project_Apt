# QA Re-triage Wave 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Every task uses superpowers:test-driven-development (failing test first). Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close GitHub issues #320, #321, #322 and the Wave 1 umbrella #323 (12 Critical + High items) in one PR to `dev`.

**Architecture:** Four independent tasks with disjoint file sets so they run in parallel on branch `fix/wave-1-critical-high`. Task A hardens the DB pool and ingestion memory/connection profile. Task B fixes route-level hazards in `sessions.py`/`chat.py`. Task C bounds the two unbounded aggregate paths and redacts a PII log line. Task D fixes two frontend flow failures.

**Tech Stack:** FastAPI + SQLAlchemy 2.x (sync engine, psycopg3), Alembic, pytest (sqlite in-memory via `backend/tests/conftest.py`), Vue 3 + Pinia + vitest.

**Spec:** `docs/planning/2026-09-19-qa-retriage.md` (verdict table, file:line evidence per item) and GitHub issues #320-#323.

## Global Constraints

- Branch: `fix/wave-1-critical-high` (already created from `dev`). Do not create sub-branches.
- Tests are sqlite in-memory (`conftest.py:54-55`). `topic_profile_json` is `Text` (`backend/db/models.py:58`), not JSONB. Any Postgres-only SQL needs a dialect branch and the sqlite path must stay tested.
- API contract: edit `docs/api/openapi.yaml` first, then run `python backend/scripts/gen_contracts.py` from repo root. CI enforces zero drift in `backend/contracts/`.
- Any new file under `backend/db/alembic/versions/` must be reviewed by the `migration-reviewer` agent before commit (hook enforces). `test_migration_chain.py` verifies a single linear chain.
- Pre-assigned Alembic revision ids (two tasks add migrations concurrently):
  - Task A: `0024_sessions_chunk_centroid`, `down_revision = "0023_worker_queue"` (verify the exact revision string inside `0023_worker_queue.py`).
  - Task C: `0025_learning_events_created_at`, `down_revision = "0024_sessions_chunk_centroid"`.
- Each task runs ONLY its own listed test files. The orchestrator runs the full `pytest` and `npm run test:unit -- --run` at the end.
- No emojis in code or comments. ASCII-only in script output. Never read `.env`.
- Commit per task with a conventional message. No AI co-author trailers, no session links.
- Preserve existing test expectations unless the item explicitly changes behaviour; when you change one, say so in the report.
- Grep with the native Grep tool or plain `grep`, not `rg` (not on PATH).

---

### Task A: DB pool timeout + ingestion connection/memory profile (F-01, F-02, F-03, F-05)

**Files:**
- Modify: `backend/config.py` (add `db_pool_timeout_s: float = 5.0` next to `db_pool_size`/`db_max_overflow` at ~42-43)
- Modify: `backend/db/database.py:24-40` (`_build_engine_kwargs`)
- Modify: `backend/services/ingestion_service.py:174-260` (`run`)
- Modify: `backend/lib/chunking.py` (streaming chunker with early abort)
- Modify: `backend/routes/upload.py:140-160` (page-count gate for `.pdf`/`.pptx`)
- Modify: `backend/services/retrieval_service.py:111-122` (`_session_centroid`)
- Modify: `backend/db/models.py` (SessionModel: `chunk_centroid` column)
- Modify: `backend/services/documents_service.py` (null `chunk_centroid` on document delete)
- Create: `backend/db/alembic/versions/0024_sessions_chunk_centroid.py`
- Test: `backend/tests/test_database_engine.py`, `test_ingestion_service.py`, `test_chunking.py`, `test_upload_route.py`, `test_retrieval_service.py`, `test_migration_chain.py`, `test_documents_service.py`

**F-01 (pool_timeout).**
- [ ] Failing test in `test_database_engine.py`: `_build_engine_kwargs("postgresql://x")["pool_timeout"] == settings.db_pool_timeout_s` and sqlite url has no `pool_timeout` key.
- [ ] Add `db_pool_timeout_s: float = 5.0` to `Settings` (env `DB_POOL_TIMEOUT_S`), document in `.env.example` next to the other `DB_POOL_*` lines, and set `kwargs["pool_timeout"] = settings.db_pool_timeout_s` in the non-sqlite branch with a one-line comment citing F-01 (shed load instead of hanging 30 s on pool exhaustion).
- [ ] Commit: `fix(db): add pool_timeout so pool exhaustion sheds instead of hanging (F-01)`

**F-02 (no session held during blob load/extract/chunk).**
- [ ] Failing test in `test_ingestion_service.py`: patch `_load_blob`/`_extract` with a fake that asserts the session has no active connection (`db.connection` not checked out -- use `db.get_bind()` pool `checkedout()` count from `engine.pool.checkedout()` equal to 0, or assert `db.in_transaction()` is False and `db.connection()` was not called via a spy). Keep the test dialect-agnostic.
- [ ] Restructure `run()`:
  1. Open session, `db.get(Document, id)`; capture `document_id`, `session_id`, `filename`, and `owner_id` (`select(SessionModel.user_id)...`) as plain locals.
  2. `db.close()` (SQLAlchemy 2.x sessions are reusable after `close()`; the next statement checks out a fresh connection).
  3. Run `_load_blob`, `_extract`, `chunking.chunk_text` with no connection held.
  4. Re-attach: `doc = db.get(Document, document_id)`; if `None`, log and return. Set `page_count`, run the chunk-cap check, `_embed_and_store`, keyword merge, mark ready.
  5. Every `except` arm must work when the failure happened in the no-session window: they already call `db.rollback()` then re-fetch; make sure they fetch the row by id (`db.get(Document, document_id)`) rather than touching the detached `doc` instance.
- [ ] Verify the existing resume/idempotency and failure-path tests in `test_ingestion_service.py` still pass.
- [ ] Commit: `fix(ingestion): release DB connection during blob load, extraction and chunking (F-02)`

**F-03 (oversized document memory).** Two layers:
- [ ] Failing tests in `test_chunking.py`: (a) `chunk_text(pages, max_chunks=3)` on input that would produce 5 chunks raises `chunking.ChunkLimitExceeded`; (b) output for normal input is identical (text, page, chunk_idx) to the pre-change output for a multi-page fixture (write the expected list by running the old function once and pasting the result).
- [ ] Rewrite `chunk_text` to stream: iterate `text_by_page`, encode one page at a time, keep a rolling token buffer plus parallel page buffer, emit a chunk whenever the buffer holds `chunk_tokens`, then drop `stride` tokens from the front. Flush the tail at the end. Add `max_chunks: int | None = None`; raise `ChunkLimitExceeded(count)` as soon as `idx >= max_chunks` so the remaining pages are never tokenised. Preserve the exact current semantics: the page of a chunk is the page of its first token, and the final partial chunk is emitted only when the previous chunk did not already end at the last token.
- [ ] In `ingestion_service.run`, pass `max_chunks=settings.max_chunks` and catch `ChunkLimitExceeded` to mark the doc failed with the existing string `"document too large to ingest (chunk limit)"` (grep tests for that string before renaming anything). Remove the now-redundant post-chunk `len(chunks) > settings.max_chunks` branch.
- [ ] Failing test in `test_upload_route.py`: a PDF whose page count exceeds `settings.max_pages` returns 413 with `code == "PAGE_LIMIT_EXCEEDED"` and `max_pages`. Build the fixture with pypdf (`PdfWriter().add_blank_page(...)` repeated).
- [ ] Add `max_pages: int = 400` to `Settings` (env `MAX_PAGES`; document in `.env.example`). In `upload.py` after the magic-bytes check: for `.pdf` use `pypdf.PdfReader(io.BytesIO(data), strict=False)` and `len(reader.pages)`; for `.pptx` open with `python-pptx` (`Presentation(io.BytesIO(data))`, `len(prs.slides)`). Wrap in `try/except Exception` and let unparseable files through (ingestion already reports extraction failures). Raise 413 `{"code": "PAGE_LIMIT_EXCEEDED", "max_pages": ..., "page_count": ...}`. Check which PDF/PPTX libraries `ingestion_service._extract_pages`/`_extract_slides` already import and use the same ones.
- [ ] Decision to record in the PR: `max_chunks` stays at 5000 (the upload-time page gate plus streaming chunker remove the memory hazard; lowering the cap would reject real textbooks).
- [ ] Commit: `fix(ingestion): stream chunking with early abort and gate PDF/PPTX page count at upload (F-03)`

**F-05 (session centroid).** Materialise lazily.
- [ ] Migration `0024_sessions_chunk_centroid.py`: `op.add_column("sessions", sa.Column("chunk_centroid", Vector(settings.embedding_dim), nullable=True))` using `pgvector.sqlalchemy.Vector`; downgrade drops it. Look at how `chunk_embeddings.embedding` is declared in an earlier migration and mirror it. Sqlite tests: check how `ChunkEmbedding.embedding` is handled on sqlite in `models.py`/`conftest.py` and mirror that (the column must not break `Base.metadata.create_all` on sqlite).
- [ ] Add `chunk_centroid` to `SessionModel` the same way.
- [ ] Failing tests in `test_retrieval_service.py`: (a) when `row.chunk_centroid` is set, `_session_centroid` returns it without issuing the `avg` query (spy on `db.execute`); (b) when it is `None` on postgresql dialect, it computes via `func.avg`, writes the result back to the row and commits. Keep the existing "returns None off-Postgres" test.
- [ ] `_session_centroid`: read `SessionModel.chunk_centroid`; if present return `list(...)`. Otherwise (postgres only) run the existing `func.avg` query, store on the row, `db.commit()`, return. `semantic_fallback_required` awaits on the event loop, so wrap the sync call: `centroid = await run_in_threadpool(_session_centroid, db, session_id)` (`from starlette.concurrency import run_in_threadpool`).
- [ ] Invalidate: in `ingestion_service.run` after `_embed_and_store`, set `SessionModel.chunk_centroid = None` for `session_id` (a new document changes the mean; the next chat turn recomputes once). In `documents_service` delete path, null it the same way. Failing test for each in `test_ingestion_service.py` / `test_documents_service.py`.
- [ ] Run `migration-reviewer` agent on the new migration before committing.
- [ ] Commit: `perf(retrieval): materialise session centroid lazily on sessions.chunk_centroid (F-05)`

**Run:** `cd backend && pytest tests/test_database_engine.py tests/test_ingestion_service.py tests/test_chunking.py tests/test_upload_route.py tests/test_retrieval_service.py tests/test_migration_chain.py tests/test_documents_service.py tests/test_ingest_in_process.py -q`

---

### Task B: Route hazards in sessions.py / chat.py (C-01, F-06, F-11)

**Files:**
- Modify: `backend/routes/sessions.py` (`_to_match` ~357, `list_sessions` 206-216, `create_session` 128, `end_session` 457, `complete_check` 669)
- Modify: `backend/routes/chat.py` (`_prepare_turn` 118, `chat_stream` 324)
- Modify: `docs/api/openapi.yaml` (GET `/api/sessions` gains `limit`/`offset` query params), then regenerate `backend/contracts/`
- Modify: `frontend/src/services/sessionsApi.js:22`, `frontend/src/__tests__/apiWrappers.test.js:45-48`
- Test: `backend/tests/test_session_lookup.py`, `test_sessions_route.py`, `test_contracts.py`, `test_chat_stream_route.py`, `test_chat_prepare_perf.py`, `test_batch6_async.py`, `test_check_complete_route.py`

**C-01 (tolerant profile parse in lookup).**
- [ ] Failing test in `test_session_lookup.py`: insert a session whose `topic_profile_json` contains an unknown extra key (e.g. `{"knowledge_level":"beginner","confirmed_gaps":[],"unknown_field":1}` plus whatever the model requires); `GET /api/sessions/lookup?topic=...` returns 200 with the match, not 500.
- [ ] Replace `TopicProfile.model_validate_json(row.topic_profile_json)` in `_to_match` with `profile_service.profile_from_row(row)` (already imported in `chat.py:237`; import the same way).
- [ ] Commit: `fix(sessions): tolerant profile parse in topic lookup (C-01)`

**F-06 (bound GET /api/sessions).** The re-triage claim "no production caller" is WRONG: `frontend/src/components/settings/ProfileTab.vue:114` calls `sessionsApi.listSessions()` for its summary line. Keep the route; bound it.
- [ ] Failing tests in `test_sessions_route.py`: `limit=201` -> 422; `limit=0` -> 422; with 5 sessions `limit=2&offset=2` returns the 3rd and 4th newest; default call with no params still returns up to 100.
- [ ] Add `limit: int = Query(100, ge=1, le=200)`, `offset: int = Query(0, ge=0)` and apply `.limit(limit).offset(offset)` to the select.
- [ ] `docs/api/openapi.yaml`: add the two query parameters to GET `/api/sessions` mirroring how `/api/sessions/library` declares them. Run `python backend/scripts/gen_contracts.py` from repo root; `test_contracts.py` must pass.
- [ ] `sessionsApi.listSessions` accepts optional `{ limit, offset }` and passes them as query params (look at how `getSessionLibrary` builds its params in the same file). Update the `apiWrappers.test.js` assertion; ProfileTab keeps calling it with no args (default 100). Note in report: ProfileTab summary now counts at most 100 topics.
- [ ] Commit: `fix(sessions): bound GET /api/sessions with limit/offset (F-06)`

**F-11 (sync DB in async handlers).** These handlers must stay `async` because they `await` LLM/summary calls and streams. Wrap the synchronous DB segments so they do not block the event loop.
- [ ] Read `test_batch6_async.py` and `test_chat_prepare_perf.py` first; they encode existing expectations about async behaviour and statement counts.
- [ ] For `_prepare_turn`: pull the synchronous body (guard reads, ensure_user, rate limit, history load, message persist -- everything except the `await` calls) into a plain `def _prepare_turn_sync(req, user_id, db, accepted_terms) -> ...` and call it with `await run_in_threadpool(...)`. If the function interleaves awaits with DB work, split into 2-3 sync segments each wrapped individually. Do not change guard order (documented in the docstring).
- [ ] For `chat_stream`: wrap any `db.execute`/`db.commit` that happens on the request coroutine (not inside the tutor task) with `run_in_threadpool`. Leave the SSE queue/disconnect loop untouched.
- [ ] For `create_session`, `end_session`, `complete_check`: same pattern -- the sync DB segments before and after each `await summary_service.generate_and_persist(...)` / stream call go through `run_in_threadpool`. `_claim_end` is a natural unit.
- [ ] Failing test (one file, `test_batch6_async.py` or a new `test_routes_threadpool.py`): patch `starlette.concurrency.run_in_threadpool` as imported in each module with a spy that still runs the function, call each endpoint via the existing TestClient fixtures, assert the spy was called at least once per handler.
- [ ] Session objects are not thread-safe for concurrent use, but sequential use from a worker thread is fine; never let two threadpool calls on the same `db` overlap (they are sequential awaits, so they do not).
- [ ] Commit: `perf(routes): run synchronous DB segments of async handlers in threadpool (F-11)`

**Run:** `cd backend && pytest tests/test_session_lookup.py tests/test_sessions_route.py tests/test_contracts.py tests/test_chat_stream_route.py tests/test_chat_prepare_perf.py tests/test_batch6_async.py tests/test_check_complete_route.py tests/test_chat.py tests/test_duplicate_topic.py tests/test_end_abandons_open_batch.py -q` and `cd frontend && npx vitest run src/__tests__/apiWrappers.test.js src/__tests__/profileTab.test.js`

---

### Task C: Bounded review queue + aggregate profile, PII redaction (F-07, F-08, G-05)

**Files:**
- Modify: `backend/routes/review.py:19-80`
- Modify: `backend/services/review_queue_service.py` (only if a helper is needed; preserve `compute_schedule` semantics)
- Modify: `backend/services/profile_service.py:466-473` (log line) and `:568-660` (`aggregate_for_user`)
- Modify: `backend/db/models.py` (`LearningEvent` index) 
- Create: `backend/db/alembic/versions/0025_learning_events_created_at.py` (`down_revision = "0024_sessions_chunk_centroid"`, created concurrently by Task A; if `test_migration_chain.py` fails only because 0024 is not yet on disk, report that rather than changing your down_revision)
- Test: `backend/tests/test_review_queue_route.py`, `test_review_queue_service.py`, `test_profile_aggregate.py`, `test_profile_service.py`, `test_profile_route.py`, `test_migration_chain.py`

**G-05 (PII in focus_clear log).**
- [ ] Failing test in `test_profile_service.py` using `caplog`: after a focus clear, no record contains the literal gap name; a record contains `focus_clear session=` and `gap_sha=` (first 8 hex of `hashlib.sha256(prior_focus.encode()).hexdigest()`) and `gap_len=`.
- [ ] Change the log call at `profile_service.py:468-473` accordingly. Keep INFO level. Confirm no other test asserts on the old format (`grep -rn "focus_clear session" backend/tests` returned nothing before this change).
- [ ] Commit: `fix(profile): redact gap name from focus_clear log line (G-05)`

**F-07 (review queue unbounded scan).** Read `review_queue_service.compute_schedule` first: `streak` = trailing consecutive correct answers per concept, so the "latest event per concept" alone is NOT sufficient; the plan is to bound the scan rather than push streak into SQL.
- [ ] Failing tests in `test_review_queue_route.py`: (a) events older than 90 days do not appear in the queue; (b) `total` equals the number of due concepts within the window; (c) `limit`/`offset` slice the sorted due list correctly (existing tests may already cover c -- keep them).
- [ ] In `get_review_queue`: add `.where(LearningEvent.created_at >= now - timedelta(days=REVIEW_WINDOW_DAYS))` with `REVIEW_WINDOW_DAYS = 90` as a module constant in `review_queue_service.py` (document in its docstring: a concept with no event in 90 days has fallen off the schedule; the cap `MAX_INTERVAL_DAYS` must be <= 90, assert that with a module-level check). Keep `compute_schedule` in Python over the windowed rows; `total` stays `len(due)` because the due list is bounded by the number of distinct concepts touched in 90 days.
- [ ] Add a composite index on `learning_events (session_id, created_at)` -- check the existing indexes on `LearningEvent` in `models.py` first; if `session_id` alone is already indexed, add `ix_learning_events_created_at` on `created_at` as the issue asks. Declare it in `models.py` (`Index(...)` in `__table_args__` or `index=True`) and in migration `0025_learning_events_created_at.py` with `op.create_index(..., if_not_exists=True)` pattern matching `0021_sessions_indexes.py`.
- [ ] Run `migration-reviewer` agent on the migration before committing.
- [ ] Commit: `perf(review): 90-day window and created_at index for review queue (F-07)`

**F-08 (aggregate_for_user full row scan).** Tests are sqlite; `topic_profile_json` is `Text`. Use a dialect branch.
- [ ] Failing tests in `test_profile_aggregate.py`: the response for a fixture of N sessions is byte-identical (all fields) before and after the change on sqlite (this is a regression pin -- generate the expected dict from the current implementation and paste it). Add a test that `aggregate_for_user` issues at most 5 statements for 50 sessions on sqlite (count via `event.listen(engine, "before_cursor_execute", ...)`), which forces the Python path to at least stop loading full ORM rows.
- [ ] Sqlite path (always tested): replace `select(SessionModel)` with `select(SessionModel.id, SessionModel.created_at, SessionModel.ended_at, SessionModel.knowledge_level_if_such_column_exists_else_topic_profile_json)` -- load only the columns the loop reads, keep `_parse_profile` per row. Compute `total`, `active`, `ended`, `last_active_at` with one aggregate statement (`count(*)`, `count(*) FILTER (WHERE ended_at IS NULL)` is Postgres-only; use `sum(case(...))` which works on both).
- [ ] Postgres path: when `db.get_bind().dialect.name == "postgresql"`, compute `mastered_counts` and `gap_counts` with SQL: `select(elem['name'].astext, count(*), min(created_at)...)` over `func.jsonb_array_elements(cast(SessionModel.topic_profile_json, JSONB)['mastered_concepts'])` as a lateral; same for `confirmed_gaps`; `first_seen_session_id` = the id of the earliest-created session containing the concept (use `array_agg(id ORDER BY created_at)[1]` or a window). Level distribution via `cast(...)['knowledge_level'].astext` grouped. Guard against malformed JSON rows: `_parse_profile` is tolerant; the SQL path must filter `topic_profile_json` values that are not valid JSON objects (`jsonb_typeof(cast(...)) = 'object'` -- an invalid JSON string would make the cast raise, so wrap the postgres path in `try/except DataError` and fall back to the Python path, logging once at WARNING).
- [ ] Because the Postgres path is not covered by sqlite tests, add a test that is skipped unless `TEST_DATABASE_URL` (check `test_pgvector_store.py` for the existing env-var name and skip pattern) points at Postgres, asserting the SQL path equals the Python path on the same fixture. Report clearly that this path is owed a live smoke.
- [ ] Leave the Issue #288 conflict resolution and `_learning_insights` as they are (they are already single bounded queries over `session_ids`), but pass `session_ids` from the aggregate query rather than from ORM rows.
- [ ] Commit: `perf(profile): aggregate_for_user loads only needed columns, SQL aggregation on Postgres (F-08)`

**Run:** `cd backend && pytest tests/test_review_queue_route.py tests/test_review_queue_service.py tests/test_profile_aggregate.py tests/test_profile_service.py tests/test_profile_route.py tests/test_migration_chain.py tests/test_focus_clear_grading_turn.py -q`

---

### Task D: Frontend start-form and onboarding hydrate failures (E-01, E-09)

**Files:**
- Modify: `frontend/src/views/HomeView.vue:6-10` and script section
- Modify: `frontend/src/stores/user.js:78-98` (`hydrateFromServer`)
- Modify: `frontend/src/router/index.js:160-182` (guard)
- Modify: `frontend/src/views/OnboardingView.vue` (template + script)
- Test: `frontend/src/__tests__/homeView.test.js`, `userStore.test.js` (or wherever `hydrateFromServer` is tested -- grep `hydrateFromServer` under `src/__tests__`), `router*.test.js` (grep `onboardingComplete` under `src/__tests__`), `onboardingView.test.js` (create if absent, mirror the mount pattern of `homeView.test.js`)

**E-01 (start form disappears on failed create).**
- [ ] Failing test in `homeView.test.js`: mount with zero sessions, make `store.createSession` reject, click start; assert the `home-quick-topic` input and `home-quick-go` button are still rendered and an element with `data-testid="home-error"` shows the friendly message. Also assert a pre-existing `store.error` from the boot-time list failure (with zero sessions) does NOT hide the form.
- [ ] In `HomeView.vue`: remove the `v-if ... v-else` gate; render the quick form unconditionally; add `const startError = ref(null)`; set it in the catch of the start handlers (`startQuick`, `startFresh`, `continuePrior`, whichever call `store.createSession`) via `friendlyError(e)`, clear it when a new start begins; render `<p v-if="startError" class="error" data-testid="home-error" role="alert">{{ startError }}</p>` under the form. Keep the existing testids.
- [ ] Update any existing `homeView.test.js` case that asserted the form is hidden when `store.error` is set (change it to assert the form stays).
- [ ] Commit: `fix(home): keep start form mounted and show start failures inline (E-01)`

**E-09 (failed hydrate force-routes to onboarding, no way out).**
- [ ] Failing tests in the user store test: after `apiGet` rejects, `hydrateFailed === true`, `hydrated === true`, `onboardingComplete` unchanged; after a success `hydrateFailed === false`.
- [ ] `user.js`: add `const hydrateFailed = ref(false)`; set `false` at the start of `hydrateFromServer`, `true` in the catch; export it.
- [ ] Failing router guard test: authenticated user, `onboardingComplete=false`, `hydrateFailed=true`, navigating to `home` is NOT redirected to `onboarding` (a user whose hydrate failed and who has no local snapshot must not be trapped). Authenticated, `onboardingComplete=false`, `hydrateFailed=false` still redirects (existing behaviour).
- [ ] `router/index.js`: add `&& !user.hydrateFailed` to the redirect condition.
- [ ] Failing `OnboardingView` test: a button/link with `data-testid="onboarding-signout"` exists; clicking it calls `authStore.signOut` and routes to `login` (see how `Sidebar.vue:285` performs sign-out and which auth store method/route name it uses; mirror it exactly).
- [ ] `OnboardingView.vue`: add a small text button under the form, "Sign out", styled like existing secondary links in `AuthCover`-based views, wired to the same sign-out flow as the sidebar.
- [ ] Commit: `fix(onboarding): do not force-route on failed hydrate, add sign-out (E-09)`

**Run:** `cd frontend && npx vitest run src/__tests__/homeView.test.js` plus the user-store, router and onboarding test files you touched, then `npm run lint`.

---

## Orchestrator wrap-up (not an executor task)

- [ ] Full `cd backend && pytest -q`; full `cd frontend && npm run test:unit -- --run`; `npm run lint`; `python backend/scripts/gen_contracts.py` then `git status` clean.
- [ ] Whole-branch review in the main loop (per feedback memory: fable reviews branches).
- [ ] PR to `dev`, body `Closes #320, #321, #322, #323`, <= 8 bullets, one verification line. State the `max_chunks` decision, the ProfileTab 100-topic cap, and the owed live smoke for the F-08 Postgres path and F-05 migration.
