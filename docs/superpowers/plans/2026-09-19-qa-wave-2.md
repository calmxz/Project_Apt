# QA Re-triage Wave 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Every task uses superpowers:test-driven-development (failing test first). Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close GitHub issue #324 (Wave 2, 19 Medium backend/ops items; Q-05 is a repo-owner GitHub UI action and stays open for the user) in one PR to `dev`.

**Architecture:** Three phases on branch `fix/wave-2-medium-backend`. Phase 0 is one sequential task that edits the API contract and pre-adds every new `Settings` field so the parallel phase never touches `openapi.yaml`, `contracts/`, or `config.py`. Phase 1 is five parallel tasks with disjoint file sets. Phase 2 is one sequential lint task (import sorting touches every file, so it must run last).

**Tech Stack:** FastAPI + SQLAlchemy 2.x (sync engine, psycopg3), Alembic, pgvector 0.8.0 (live Supabase), pytest (sqlite in-memory via `backend/tests/conftest.py`), Vue 3 + vitest (frontend untouched in this wave).

**Spec:** `docs/planning/2026-09-19-qa-retriage.md` (verdict table, file:line evidence per item) and GitHub issue #324. Wave 1 precedent: `docs/superpowers/plans/2026-09-19-qa-wave-1.md`.

## Global Constraints

- Branch: `fix/wave-2-medium-backend` (already created from `dev`). Do not create sub-branches or worktrees.
- Tests are sqlite in-memory (`conftest.py`). Any Postgres-only SQL needs a dialect branch (`db.get_bind().dialect.name == "postgresql"`) and the sqlite path must stay tested.
- API contract: `docs/api/openapi.yaml` is edited ONLY in Phase 0 and by Task E. Regenerate with `python backend/scripts/gen_contracts.py` from repo root. CI enforces zero drift in `backend/contracts/`.
- `backend/config.py` and `.env.example`: new `Settings` fields are added ONLY in Phase 0. In Phase 1 only Task D (G-12) edits `.env.example`; nobody edits `config.py`. If you need a setting Phase 0 did not add, report it, do not add it.
- `docs/reference.md` and `docs/decisions.md`: NO executor writes them. Report the text you want recorded; the orchestrator writes both at wrap-up.
- Any new file under `backend/db/alembic/versions/` must be reviewed by the `migration-reviewer` agent before commit (hook enforces). `test_migration_chain.py` verifies a single linear chain. Revision ids MUST be <= 32 characters (alembic_version.version_num is varchar(32); Wave 1 failed CI on a 35-char id). Pre-assigned:
  - Task B: `0026_documents_content_sha256`, `down_revision = "0025_learning_events_created_at"`.
  - Task B: `0027_chat_messages_sid_id_desc`, `down_revision = "0026_documents_content_sha256"`.
  - Copy the shape of `0025_learning_events_created_at.py`: `autocommit_block`, invalid-namesake drop guard, `if_not_exists`, `postgresql_concurrently=True`, and open with `op.execute("SET lock_timeout = '5s'")` on hot tables (C-12).
- Each task runs ONLY its own listed test files. The orchestrator runs the full `pytest` and `npm run test:unit -- --run` at the end.
- No emojis in code or comments. ASCII-only in script output. Never read `.env`.
- Commit per item with a conventional message (`fix(scope): ... (ID)`). No AI co-author trailers, no session links.
- Preserve existing test expectations unless the item explicitly changes behaviour; when you change one, say so in the report.
- Grep with the native Grep tool or plain `grep`, not `rg` (not on PATH). A zero-hit result from `rtk`-wrapped grep is unreliable; confirm with the Grep tool.
- Ruff is NOT configured until Phase 2. Do not add `# noqa` for it.

---

## Phase 0 (sequential, one executor)

### Task 0: Contract edits + Settings pre-adds + codegen (C-07/C-09/C-10/C-13 contract halves)

**Files:**
- Modify: `docs/api/openapi.yaml`
- Modify: `backend/config.py`
- Regenerate: `backend/contracts/` (do not hand-edit)
- Test: `backend/tests/test_contracts.py`, `backend/tests/test_contracts_no_subjects.py`

**openapi.yaml edits** (anchors as of this plan; re-locate by name):
- [ ] `ChatRequest.message` (~line 1254): add `minLength: 1` (C-10).
- [ ] `SessionCreateRequest.topic` (~1265) and `SessionUpdateRequest.topic` (~1279): add `minLength: 1` (C-09).
- [ ] `POST /api/upload` (~542, responses ~559-583): add `409` (`session_ended`, same description style as `/api/chat/stream`'s 409) and `404` (session not found) (C-07, C-13).
- [ ] `PATCH /api/sessions/{session_id}` (~289): add `409` duplicate_topic (C-13). Mirror the 409 block already on `POST /api/sessions`.
- [ ] `POST /api/sessions` (~93): add `422` (declared_level forbidden when seed_mode=resume) (C-13).
- [ ] `PATCH /api/me` (~249): add `422` (empty patch) (C-13).
- [ ] Keep `openapi-spec-validator` happy: `python -m openapi_spec_validator docs/api/openapi.yaml` from repo root (CI runs it).

**config.py pre-adds** (with a one-line comment each, citing the item id; defaults are load-bearing, do not change them):
- [ ] `max_json_body_bytes: int = 64 * 1024` (C-03: non-multipart request bodies above this are rejected before parsing).
- [ ] `llm_turn_reserve_usd: float = 0.02` (B-05: provisional per-turn charge reserved at the cost gate and released when the turn ends).
- [ ] `citation_min_similarity: float = 0.25` (G-11: cosine-similarity floor for surfacing a retrieved chunk as a citation; None-scored chunks pass).
- [ ] `hnsw_ef_search: int = 100` (F-10: `SET LOCAL hnsw.ef_search` for the retrieval transaction; pgvector default 40).
- [ ] `readiness_timeout_s: float = 2.0` (G-07: `/ready` DB probe budget).

**Codegen + verify:**
- [ ] `python backend/scripts/gen_contracts.py` from repo root; confirm `backend/contracts/models.py` now has `constr(min_length=1, max_length=4000)` for `message` and `min_length=1` on both `topic` fields.
- [ ] Run: `cd backend && pytest tests/test_contracts.py tests/test_contracts_no_subjects.py tests/test_chat.py tests/test_sessions_route.py -q`. If an existing test posts an empty `message` or `topic` and expected something other than 422, report it (do not fix; Task A / Task E own those routes).
- [ ] Commit: `chore(contracts): document wave-2 response codes, minLength on message/topic, pre-add settings`

---

## Phase 1 (parallel, disjoint files)

### Task A: Chat turn cost gate, empty-message guard, connection release, citation floor (B-05, C-10, F-13, G-11)

**Files:**
- Modify: `backend/services/cost_meter.py`
- Modify: `backend/routes/chat.py`
- Modify: `backend/agent/tutor.py` (citation builder only, ~438; and the release point if it lives there)
- Create: `backend/lib/citations.py`
- Test: `backend/tests/test_cost_meter.py`, `test_cost_cap.py`, `test_cost_guard.py`, `test_chat.py`, `test_chat_prepare_perf.py`, `test_chat_stream_route.py`, `test_batch6_async.py`, `test_tutor_stream.py`, `test_tutor_loop.py`, and a new `test_citation_floor.py`

**B-05 (fold the cost gate into an atomic write).** Today `_prepare_turn_guards` (`chat.py:~137-150`) reads today's spend with a plain SELECT and compares; N concurrent turns at cap-minus-epsilon all pass and each runs an LLM call. Replace the read gate with a reservation:
- [ ] Failing tests in `test_cost_meter.py`: (a) `reserve_cost(db, user, Decimal("0.02"))` returns the PRE-increment total and the ledger row is incremented by 0.02; (b) `adjust_cost(db, user, Decimal("-0.02"))` decrements (signed; negative allowed; `0` is a no-op); (c) two sequential `reserve_cost` calls at `hard_cap - 0.01` with reserve 0.02: first returns pre < hard_cap, second returns pre >= hard_cap.
- [ ] `cost_meter.reserve_cost(db, user_id, amount) -> Decimal`: same INSERT .. ON CONFLICT DO UPDATE .. RETURNING shape as `record_cost`; return `new_total - amount` (the pre-increment total). `adjust_cost(db, user_id, delta)`: same upsert, delta may be negative, returns new total. Do not floor at zero (the reserve was added by us, so subtracting it cannot go negative). Keep `record_cost` unchanged for existing callers.
- [ ] Failing test in `test_chat.py` (or `test_chat_stream_route.py`): with spend seeded at `hard_cap - 0.01` and two prepared turns for the same user, the second is rejected with 429 `daily_cost_cap_reached` even though the first has not yet recorded any real cost. (Drive `_prepare_turn` twice via the existing fixtures; the LLM is stubbed.)
- [ ] In `_prepare_turn_guards`: replace the `spend_subquery` read with `pre = cost_meter.reserve_cost(db, user_id, Decimal(str(settings.llm_turn_reserve_usd)))`; `cost_status = check_cap_from_spend(pre)`; reject exactly as today when `not cost_status.allowed`. Keep the `user_exists` subquery (one extra statement is acceptable; `test_chat_prepare_perf.py` pins statement counts, update the pinned number with a comment explaining the +1). On the immediate reject path do NOT subtract back: nothing is committed yet and `get_db`'s `close()` discards it. The ONE leak is a rate-limit reject after `ensure_user` has committed (step 3 then step 4-5): call `adjust_cost(db, user_id, -reserve)` + `db.commit()` in that reject arm.
- [ ] Release exactly once per prepared turn: in `chat_stream`'s `finally` (after the tutor task has finished or been cancelled; it shares `ctx.db`, never overlap), `adjust_cost(db, user_id, -reserve)` + `db.commit()` via `run_in_threadpool`. Cover normal completion, client disconnect/cancel, and tutor exception. If `_prepare_turn` raises after the reservation was committed by `ensure_user`/`check_and_increment` but before the stream starts, release there too. Failing test: after a full stubbed turn, the ledger total equals the recorded real cost with no 0.02 residue.
- [ ] Tutor tests mock `record_cost`; they will not exercise the release. Say so in the report.
- [ ] Report text for the PR: the reserve inflates the usage ledger by `llm_turn_reserve_usd` for the duration of a turn; a process crash mid-turn leaves the reserve on the ledger until UTC midnight.
- [ ] Commit: `fix(chat): reserve per-turn spend atomically at the cost gate (B-05)`

**C-10 (empty message).** `minLength: 1` now in the contract (Phase 0). Whitespace-only still passes.
- [ ] Failing test in `test_chat.py`: `POST /api/chat/stream` with `message: "   "` returns 422 and persists no `ChatMessage`; with `message: ""` returns 422 (contract).
- [ ] First statement of `_prepare_turn` (before the threadpool guards): `if not req.message.strip(): raise HTTPException(status_code=422, detail={"code": "empty_message"})`. Do not strip the stored message.
- [ ] Commit: `fix(chat): reject whitespace-only messages before any guard side effect (C-10)`

**F-13 (release the connection across the awaited embedding calls).** After Wave 1's F-11 split, `_prepare_turn_context` opens a transaction (history select) and nothing commits before the awaited `semantic_fallback_required` / `prefetch_for_prompt` calls; the first commit is the user-message persist.
- [ ] Failing test in `test_chat_prepare_perf.py` or `test_batch6_async.py`: after `_prepare_turn_context` returns, `db.in_transaction()` is False (spy on `db.commit` or check `db.get_transaction()`).
- [ ] Add `db.commit()` as the last statement of `_prepare_turn_context` with a comment citing F-13 (read-only segment; commit releases the pooled connection while the two embedding round-trips await; `session` is already expunged so nothing expires that is read later).
- [ ] Commit: `perf(chat): release DB connection before awaited embedding calls (F-13)`

**G-11 (citation similarity floor).** `score` on a retrieved chunk is pgvector cosine DISTANCE (0 best); `similarity = 1 - score`. `score` is `None` on the sqlite fallback path and must pass through.
- [ ] Failing tests in new `test_citation_floor.py`: chunks with `score` such that similarity < `settings.citation_min_similarity` are dropped; similarity >= floor kept; `score=None` kept; output `Citation` fields identical to today's builders (`doc_id` str, `text`, `page`, `doc_name`).
- [ ] Create `backend/lib/citations.py` with `chunks_to_citations(chunks: list[dict]) -> list[Citation]` implementing the above (convert once; log at DEBUG how many were dropped).
- [ ] Replace both inline builders (`tutor.py` ~438 in the `retrieve_chunks` result arm and `chat.py` ~360 `prefetched_citations`) with the helper. Keep F-56 dedup logic in tutor.py untouched; only the list construction changes. Do NOT filter the chunks handed to the model, only the citations.
- [ ] Run `test_tutor_stream.py` in full; some tests build chunks without `score` (they must still pass, see None rule) and some may set a `score` that now falls under the floor -- adjust the fixture score, not the floor, and say so.
- [ ] Commit: `fix(citations): apply similarity floor before surfacing retrieved chunks as citations (G-11)`

**Run:** `cd backend && pytest tests/test_cost_meter.py tests/test_cost_cap.py tests/test_cost_guard.py tests/test_chat.py tests/test_chat_prepare_perf.py tests/test_chat_stream_route.py tests/test_batch6_async.py tests/test_tutor_stream.py tests/test_tutor_loop.py tests/test_citation_floor.py tests/test_usage_service.py -q`

---

### Task B: Upload guards, content dedupe, chat_messages index (C-07, C-08, F-09, C-12 migration half)

**Files:**
- Modify: `backend/routes/upload.py`
- Modify: `backend/db/models.py` (`Document`: `content_sha256` + unique index; `ChatMessage`: index)
- Create: `backend/db/alembic/versions/0026_documents_content_sha256.py`
- Create: `backend/db/alembic/versions/0027_chat_messages_sid_id_desc.py`
- Test: `backend/tests/test_upload_route.py`, `test_migration_chain.py`, `test_documents_service.py`, `test_sessions_route.py`

**C-07 (upload into an ended session).**
- [ ] Failing test in `test_upload_route.py`: uploading to a session with `ended_at` set returns 409 `{"code": "session_ended"}`, consumes no rate-limit slot, creates no `Document` row.
- [ ] In `upload_file` immediately after the ownership 404 (before the cost gate): `if sess.ended_at is not None: raise HTTPException(status_code=409, detail={"code": "session_ended"})`. Same detail shape as `chat.py`.
- [ ] Commit: `fix(upload): reject uploads into ended sessions with 409 (C-07)`

**C-08 (idempotent re-upload).**
- [ ] `models.py`: `Document.content_sha256: Mapped[str | None] = mapped_column(String(64), nullable=True)` (legacy rows stay NULL). Add to `__table_args__` a partial unique index `uq_documents_session_sha` on `(session_id, content_sha256)` with `postgresql_where=text("content_sha256 IS NOT NULL AND status <> 'failed'")` and the same `sqlite_where`, so a failed row never blocks a retry and NULLs never collide.
- [ ] Migration `0026_documents_content_sha256.py`: `SET lock_timeout = '5s'`; `op.add_column` (nullable, no default: no rewrite); then the partial unique index built CONCURRENTLY inside `autocommit_block` with the 0025-style invalid-namesake guard (`CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS ... WHERE ...` via `op.execute`, since `op.create_index` cannot express the partial WHERE portably -- use the dialect branch: raw SQL on postgresql, `op.create_index(..., sqlite_where=...)` elsewhere). Downgrade drops both.
- [ ] Failing tests in `test_upload_route.py`: (a) uploading identical bytes twice to the same session returns 202 both times with the SAME `document_id`, one `Document` row, one blob write (spy on `object_store.get_store().put`); (b) same bytes into a different session creates a second row; (c) a prior row with `status="failed"` and the same sha does not block: a new row is created; (d) `content_sha256` is populated on the created row; (e) the second identical upload does not consume a rate-limit slot (check `usage_counters`) -- do the sha lookup after the ownership/ended checks and before the cost gate and rate limiter.
- [ ] `upload_file`: read the body (existing `_read_bounded`), compute `hashlib.sha256(data).hexdigest()`. NOTE the body read currently happens after the rate limiter; reorder so the dedupe lookup runs before the cost gate and rate limiter (page gate and magic-byte checks may stay where they are or move with the read; keep the plaintext chunk estimate before any write). Lookup: `select(Document).where(session_id==, content_sha256==, status != "failed")`; if found return `UploadResponse(document_id=existing.id, session_id, filename=existing.filename, status=existing.status)` with 202 and the `X-Cost-Warning` header as today. Otherwise create the row with `content_sha256=sha`. Wrap the `db.flush()` in `try/except IntegrityError`: rollback, re-select by `(session_id, sha)`, return that row's payload (two concurrent identical uploads). Test (f): simulate the race by inserting the conflicting row from a second session object between the lookup and the flush (monkeypatch the lookup helper to insert-then-return-None).
- [ ] Run `migration-reviewer` on 0026 before committing.
- [ ] Commit: `feat(upload): dedupe identical uploads per session via content_sha256 (C-08)`

**F-09 (chat_messages index).** The learning_events half is CLOSED by Wave 1's `0025_learning_events_created_at` (`created_at` single column; the review query at `routes/review.py:38-45` filters `created_at >= window` and orders `(created_at, id)` over `session_id IN (...)`, so a composite `(created_at, id)` buys nothing measurable). Remaining: `sessions.py:~229` orders history by `ChatMessage.id.desc()` and only `ix_chat_messages_session_created (session_id, created_at)` exists.
- [ ] `models.py` `ChatMessage.__table_args__`: add `Index("ix_chat_messages_sid_id_desc", "session_id", text("id DESC"))` (check how the existing session_created index is declared -- migration-only or also in the model -- and match; if indexes live only in migrations, declare it there only).
- [ ] Migration `0027_chat_messages_sid_id_desc.py`: `SET lock_timeout = '5s'` (chat_messages is hot), `CREATE INDEX CONCURRENTLY IF NOT EXISTS ix_chat_messages_sid_id_desc ON chat_messages (session_id, id DESC)` inside `autocommit_block` with the invalid-namesake guard, sqlite branch via `op.create_index` (sqlite accepts `id DESC` in an index expression). Downgrade drops it.
- [ ] Failing test: `test_migration_chain.py` still passes with both new revisions and the head is `0027_chat_messages_sid_id_desc`; a test that `alembic upgrade head` on sqlite (if such a fixture exists in the test suite -- grep `command.upgrade`) creates the index.
- [ ] Run `migration-reviewer` on 0027 before committing.
- [ ] Report for the PR: the learning_events `(created_at, id)` deviation and why.
- [ ] Commit: `perf(db): index chat_messages (session_id, id DESC) for history loads (F-09)`

**Run:** `cd backend && pytest tests/test_upload_route.py tests/test_migration_chain.py tests/test_documents_service.py tests/test_sessions_route.py tests/test_ingest_in_process.py -q`

---

### Task C: Retrieval transaction tuning + safe ingestion error strings + body-size middleware (F-10, C-11, C-03)

**Files:**
- Modify: `backend/services/pgvector_store.py`
- Modify: `backend/services/ingestion_service.py`
- Create: `backend/lib/body_limit.py`
- Modify: `backend/main.py` (one `add_middleware` line)
- Test: `backend/tests/test_pgvector_store.py`, `test_pgvector_retrieval.py`, `test_ingestion_service.py`, `test_main_lifespan.py`, new `test_body_limit.py`

**F-10 (HNSW filtered-scan tuning).** Decision (record in report, orchestrator writes decisions.md): do NOT denormalise `doc_ready` onto `chunk_embeddings`. The join to `documents` is needed anyway for `Document.filename`, the status predicate is a PK-joined filter per candidate, and pgvector 0.8.0 (confirmed live on Supabase) solves the filtered-HNSW under-fetch with iterative scan. Partitioning is a note, not code.
- [ ] Failing tests in `test_pgvector_store.py`: on a postgresql-dialect bind (mock `db.get_bind().dialect.name`), `query_chunks` executes `SET LOCAL hnsw.ef_search = 100` and `SET LOCAL hnsw.iterative_scan = strict_order` BEFORE the select, in the same transaction (spy on `db.execute` call order); on sqlite no SET statements are issued; `ef_search` is rendered as a validated int (a non-int setting value raises `ValueError` rather than being interpolated).
- [ ] In `query_chunks`, before building the select: `if db.get_bind().dialect.name == "postgresql": db.execute(text(f"SET LOCAL hnsw.ef_search = {int(settings.hnsw_ef_search)}")); db.execute(text("SET LOCAL hnsw.iterative_scan = strict_order"))`. `SET LOCAL` is transaction-scoped: it must run right before the search in the same transaction (Task A's F-13 commit means a fresh transaction starts here, which is fine). Wrap the two SETs in `try/except ProgrammingError` that logs once at WARNING and continues (a pgvector < 0.8 would reject `iterative_scan`).
- [ ] Report text for reference.md: `hnsw.ef_search`/`iterative_scan` semantics, why `strict_order`, and a partitioning note (`chunk_embeddings` LIST-partitioned by `session_id` hash bucket is the future path once the table exceeds the HNSW build memory budget; not needed at current scale).
- [ ] Commit: `perf(retrieval): SET LOCAL hnsw.ef_search + iterative_scan on the search transaction (F-10)`

**C-11 (no `str(e)` on `documents.error`).** `documents.error` is rendered verbatim by the frontend (`ReferenceStatusBanner.vue:84`, `SessionView.vue:902`), so keep it human-readable but make it an enumerated fixed set; the raw exception goes to the log line only.
- [ ] Failing test in `test_ingestion_service.py`: when `_extract` raises `RuntimeError("/srv/secret/path: boom")`, `doc.error` does not contain `"secret"` and equals `ingestion_service.ERR_EXTRACTION_FAILED`; when `_embed_and_store` raises a generic exception, `doc.error == ERR_EMBEDDING_FAILED`; any other stage -> `ERR_INGESTION_FAILED`; the log record contains the original message.
- [ ] Add module constants `ERR_EXTRACTION_FAILED = "text extraction failed"`, `ERR_EMBEDDING_FAILED = "embedding failed"`, `ERR_INGESTION_FAILED = "ingestion failed"`, plus the three already-fixed strings (`chunk limit`, `cost cap`, and upload.py's `"storage write failed"` stays where it is) collected into `INGEST_ERROR_MESSAGES: frozenset`. Track the current stage in a local (`stage = "extract" | "embed" | "other"`) and map in the generic `except Exception` arm. Update the module docstring (~line 38) that still describes `str(e)` as the contract. Add a test asserting every assigned `doc.error` in the module is in the frozenset (grep-based or by exercising each arm).
- [ ] Commit: `fix(ingestion): persist enumerated error strings, keep exception text in logs only (C-11)`

**C-03 (request body size gate).** Pure ASGI middleware (NOT `BaseHTTPMiddleware`: SSE on `/chat/stream` must stay unbuffered). Mirror `backend/lib/request_id.py`.
- [ ] Failing tests in new `test_body_limit.py` using the app TestClient: (a) `POST /api/chat/stream` with a JSON body of `max_json_body_bytes + 1` bytes and a correct `Content-Length` -> 413 `{"detail": {"code": "body_too_large", "max_bytes": ...}}` and the route handler is never entered (spy); (b) same size sent chunked without Content-Length -> 413 once the running count exceeds the limit; (c) a `multipart/form-data` POST above the limit is NOT rejected by this middleware (the upload route's own gate handles it); (d) a normal 2 KB JSON body passes; (e) a GET with no body passes; (f) an SSE response still streams (reuse an existing streaming test or assert the response headers are not buffered).
- [ ] `backend/lib/body_limit.py`: `class BodySizeLimitMiddleware` with `__init__(self, app, max_bytes)`. On `http` scope: if the `content-type` header starts with `multipart/form-data`, pass through. If `content-length` parses and exceeds `max_bytes`, send a 413 JSON response immediately (do not call the app). Otherwise wrap `receive`: accumulate `len(message["body"])` for `http.request` messages; when the total exceeds `max_bytes`, if the response has not started, send the 413 and stop feeding the app (raise a private exception that `__call__` catches after sending); if the response already started, close the connection. Non-http scopes pass through.
- [ ] `main.py`: `app.add_middleware(BodySizeLimitMiddleware, max_bytes=settings.max_json_body_bytes)` next to `RequestIdMiddleware` (order: body limit outermost so an oversized body never reaches the request-id middleware's app call; verify `test_main_lifespan.py` still passes).
- [ ] Commit: `fix(api): reject oversized non-multipart request bodies before parsing (C-03)`

**Run:** `cd backend && pytest tests/test_pgvector_store.py tests/test_pgvector_retrieval.py tests/test_ingestion_service.py tests/test_main_lifespan.py tests/test_body_limit.py tests/test_retrieval_service.py -q`

---

### Task D: Auth cache lock, coarse tool errors, readiness probe, ops hardening (F-19, G-04, G-07, G-10, G-12)

**Files:**
- Modify: `backend/services/auth.py`
- Modify: `backend/agent/tools.py` (the `except Exception` arm at ~110-112 only)
- Modify: `backend/routes/health.py`
- Modify: `render.yaml`
- Modify: `.github/workflows/backup.yml`
- Modify: `.env.example`
- Test: `backend/tests/test_auth_dependency.py`, `test_auth_config_batch6.py`, `test_tools_dispatch_logging.py`, `test_dispatch_session_id_injection.py`, `test_health.py`, `test_deploy_config.py`

**F-19 (JWKS refresh race).**
- [ ] Failing test in `test_auth_dependency.py`: with the cache expired, 20 threads calling `_get_jwks_client()` concurrently construct `PyJWKClient` exactly once (monkeypatch the class with a counting fake); the constructor is called with `cache_jwk_set=True, lifespan=3600`.
- [ ] `auth.py`: add `_JWKS_LOCK = threading.Lock()`; in `_get_jwks_client` do a lock-free staleness read, then `with _JWKS_LOCK:` re-check (double-checked) before constructing `PyJWKClient(settings.supabase_jwks_url, cache_jwk_set=True, lifespan=_JWKS_TTL_SECONDS)`. Apply the same explicit kwargs to the boot warm-up client in `validate_jwks_startup`. The `auth_not_configured` 500 path stays.
- [ ] Commit: `fix(auth): lock JWKS client refresh and pin PyJWKClient cache settings (F-19)`

**G-04 (coarse tool error).** Read `test_tools_dispatch_logging.py` and `test_dispatch_session_id_injection.py` FIRST; they may assert on today's `str(e)` payload.
- [ ] Failing test: a tool that raises `ValueError("session mismatch for sess-123")` yields `ToolResult(ok=False, status="failed", error="tool_failed")` and the WARNING log record contains `sess-123`; a `pydantic.ValidationError` from `model_validate` still returns its own message in `error` (the model needs it to repair the call).
- [ ] `tools.py`: split the arm into `except ValidationError as e: log.warning(...); return ToolResult(..., error=str(e))` and `except Exception as e: log.warning("tool dispatch failed name=%s error=%s", name, e); return ToolResult(..., error="tool_failed")`. Add `TOOL_FAILED = "tool_failed"` to `lib/error_codes.py`. The unknown-tool string stays.
- [ ] Commit: `fix(tools): return coarse tool_failed code, keep exception text in logs (G-04)`

**G-07 (readiness probe).**
- [ ] Failing tests in `test_health.py`: `GET /ready` returns 200 `{"status": "ok"}` when `SELECT 1` succeeds; returns 503 `{"status": "unavailable"}` when the DB call raises; returns 503 when the probe exceeds `settings.readiness_timeout_s` (monkeypatch the probe to sleep). `/health` and `/healthz` unchanged.
- [ ] `health.py`: `async def ready(db: Session = Depends(get_db))` running `await asyncio.wait_for(run_in_threadpool(lambda: db.execute(select(1)).scalar_one()), timeout=settings.readiness_timeout_s)`; any exception (including `TimeoutError`) -> `JSONResponse(status_code=503, content={"status": "unavailable"})`. Check whether `HealthResponse.status` is an enum in the contract; if so use a plain `JSONResponse` for the 503 and keep the 200 on the model. `include_in_schema=False` is acceptable (no openapi edit in this task).
- [ ] `render.yaml`: `healthCheckPath: /ready`. Update `test_deploy_config.py::test_render_service_shape`.
- [ ] Report text for the PR: repointing Render's health check means a Supabase outage now fails the instance health check (Render restarts the instance) instead of serving 500s; takes effect at the next deploy.
- [ ] Commit: `feat(health): add /ready DB probe and point Render health check at it (G-07)`

**G-10 (backup workflow timeout + failure alert).**
- [ ] `backup.yml`: `timeout-minutes: 15` on the `backup` job; `permissions: contents: read, issues: write`; a final step `if: failure()` that uses the preinstalled `gh` CLI (`GH_TOKEN: ${{ github.token }}`) to open an issue titled `db-backup failed <run date>` linking `${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}` -- or comment on an existing open issue with label `backup-failure` if one exists (`gh issue list --label backup-failure --state open`). No new third-party actions (SHA-pin rule).
- [ ] Failing test in `test_deploy_config.py`: parse `.github/workflows/backup.yml`; assert `jobs.backup.timeout-minutes == 15`, `permissions.issues == "write"`, and a step with `if: failure()` exists.
- [ ] Commit: `ops(backup): bound the job and open an issue on failure (G-10)`

**G-12 (env template / render drift).**
- [ ] Failing test in `test_deploy_config.py`: every field name in `config.Settings.model_fields` (uppercased) appears in `.env.example` as `NAME=` or `# NAME=`; every key in `render.yaml` `envVars` is a known `Settings` field (uppercased) -- assert both directions with a small explicit allowlist for template-only names if any exist (there should be none). Import `Settings` from `config` the way other tests do (conftest sets the path).
- [ ] `.env.example`: add every missing var as a commented default with a one-line description, grouped under existing headings: ENV, DB_POOL_SIZE, DB_MAX_OVERFLOW, LLM_TEMPERATURE, SUMMARY_TEMPERATURE, RETRIEVAL_FALLBACK_THRESHOLD, LLM_TIMEOUT_S, SUMMARY_TIMEOUT_S, EMBEDDING_TIMEOUT_S, MAX_PROFILE_LIST, DEBUG_TIMING, SUPABASE_JWKS_URL_OVERRIDE, INGEST_IN_PROCESS, BURST_LIMIT_PER_MINUTE, LLM_RETRY_ATTEMPTS, LLM_RETRY_BASE_DELAY_S, and the five Phase 0 additions (MAX_JSON_BODY_BYTES, LLM_TURN_RESERVE_USD, CITATION_MIN_SIMILARITY, HNSW_EF_SEARCH, READINESS_TIMEOUT_S). Let the test tell you what else is missing.
- [ ] `render.yaml`: pin `MODEL`, `EMBEDDING_MODEL`, `DB_POOL_SIZE`, `DB_MAX_OVERFLOW` with their current defaults as explicit `value:` entries.
- [ ] Commit: `ops(config): env template and render.yaml cover every Settings field, drift test (G-12)`

**Run:** `cd backend && pytest tests/test_auth_dependency.py tests/test_auth_config_batch6.py tests/test_tools_dispatch_logging.py tests/test_dispatch_session_id_injection.py tests/test_health.py tests/test_deploy_config.py tests/test_tutor_stream.py -q`

---

### Task E: Session topic guards + documented-status-code reachability test (C-09, C-13)

Sole Phase 1 owner of `docs/api/openapi.yaml` and `backend/routes/sessions.py`.

**Files:**
- Modify: `backend/routes/sessions.py` (`create_session` ~218, `update_session` ~660)
- Modify: `docs/api/openapi.yaml` (only if the reachability test finds codes beyond the four Phase 0 documented), then regenerate contracts
- Create: `backend/tests/test_openapi_status_codes.py`
- Test: `backend/tests/test_sessions_route.py`, `test_duplicate_topic.py`, `test_contracts.py`

**C-09 (whitespace-only topic).**
- [ ] Failing tests in `test_sessions_route.py`: `POST /api/sessions` with `topic: "   "` -> 422 `{"code": "empty_topic"}` and no row; `PATCH /api/sessions/{id}` with `topic: "  "` -> 422 and the row is unchanged; `topic: ""` -> 422 (contract, from Phase 0).
- [ ] Guard before any side effect in both handlers: `topic = req.topic.strip(); if not topic: raise HTTPException(422, detail={"code": "empty_topic"})`; use `topic` downstream. In `create_session` this must run before the duplicate-topic lookup and the prior-session work.
- [ ] Commit: `fix(sessions): reject whitespace-only topics on create and update (C-09)`

**C-13 (reachability test).** Phase 0 documented the four known codes. Build the CI check that keeps it true.
- [ ] `test_openapi_status_codes.py`: load `docs/api/openapi.yaml`; for each module in `backend/routes/*.py`, `ast.parse` it; find the router prefix (`APIRouter(prefix=...)`); for each function decorated with `@router.<get|post|patch|put|delete>("<path>", ...)`, collect the set of integer status codes from `HTTPException(status_code=<int>)` / `status.HTTP_<n>_...` / `JSONResponse(status_code=<int>)` in its body AND in the bodies of same-module functions it calls by name (one level, plus one more level for helpers prefixed `_` -- `chat.py`'s guards live two helpers down). Assert that set is a subset of the codes documented under `paths[prefix+path][method].responses`, ignoring 200/201/202 (success codes are the `status_code=` decorator arg) and treating a documented `default` as wildcard-free (do not count it). Print a readable diff on failure: `route -> raised but undocumented {..}`.
- [ ] Run it. It will likely surface codes beyond the four (e.g. `409` on `/api/sessions/{id}/reopen`, `422 empty_topic` from C-09, `429`/`413`/`507` already documented). For each real gap: document it in `openapi.yaml`, regenerate contracts, `test_contracts.py` green. For a false positive (dead branch, helper shared across routes) tighten the walker rather than allowlisting; if an allowlist is unavoidable keep it as an explicit `KNOWN_GAPS` dict at the top of the test with a one-line reason per entry.
- [ ] Do not document `500` anywhere (unhandled), and do not add codes for `Depends`-level rejections that live in `services/auth.py` / `velocity_limit.py` (401/429 are already documented per path; if the walker sees them through a dependency, exclude dependency modules).
- [ ] Commit: `test(api): every raised status code must be documented under its path (C-13)`

**Run:** `cd backend && pytest tests/test_sessions_route.py tests/test_duplicate_topic.py tests/test_contracts.py tests/test_openapi_status_codes.py tests/test_check_complete_route.py -q`

---

## Phase 2 (sequential, after all Phase 1 tasks are committed)

### Task F: Ruff lint in dev extras + CI, clear the backlog (Q-03)

**Files:**
- Modify: `backend/pyproject.toml` (dev extras + `[tool.ruff]`)
- Modify: `.github/workflows/ci.yml` (backend job: one `ruff check .` step after Install)
- Modify: whatever `ruff check --fix` and the manual fixes touch under `backend/`

Baseline measured 2026-09-19 with `--isolated --select E,F,B,I --line-length 100`: ~110 `I001`, 28 `B008`, 12 `E741`, 11 `B904`, 5 `E402`, 3 `F401`, 2 `B905`, 1 `B023`, 75 `E501`.

- [ ] `pyproject.toml`: add `"ruff>=0.14,<1.0"` to `dev`; add
  ```toml
  [tool.ruff]
  line-length = 100
  target-version = "py312"
  extend-exclude = ["contracts/models.py"]

  [tool.ruff.lint]
  select = ["E", "F", "B", "I"]
  ignore = ["B008", "E501"]

  [tool.ruff.lint.per-file-ignores]
  "db/alembic/env.py" = ["E402"]
  "db/alembic/versions/*.py" = ["E402"]
  "tests/*" = ["E402"]
  ```
  Adjust `extend-exclude`/per-file-ignores only for genuinely generated or path-manipulating files; check the E402 sites first (`ruff check . --select E402`).
- [ ] Commit 1 (autofix only): `ruff check . --fix` (I001, F401). Verify `pytest -q` in full. Commit: `style(backend): ruff autofix (import order, unused imports) (Q-03)`
- [ ] Commit 2 (manual): B904 -> add `from e` / `from None` deliberately; B905 -> `strict=False` where lengths legitimately differ, `strict=True` only when proven equal, or ignore the rule if in doubt; B023 -> bind the loop variable; E741 -> rename `l`/`O`/`I` locals. Do NOT change behaviour. Full `pytest -q`. Commit: `fix(backend): resolve ruff B/E findings without behaviour change (Q-03)`
- [ ] `ci.yml` backend job: after `Install`, add `- name: Lint (ruff)` / `run: ruff check .` (working-directory already `backend`). Commit: `ci(backend): run ruff check (Q-03)`
- [ ] Report text for the PR: the retriage asked for a dedicated backlog PR; it is folded here because import-sorting after the fact would conflict with every Phase 1 file.

**Run:** `cd backend && ruff check . && pytest -q`

---

## Orchestrator wrap-up (not an executor task)

- [ ] Write `docs/reference.md` sections: Migrations (C-12 checklist from `.claude/agents/migration-reviewer.md:29-40`), Retrieval (F-10 text from Task C), Cost gate (B-05 reserve semantics from Task A).
- [ ] Write `docs/decisions.md` entries: F-10 denormalisation deferred; F-09 learning_events composite index not added; C-11 human-readable enumerated strings instead of codes (frontend renders `doc.error`); Q-03 folded into the wave PR.
- [ ] Full `cd backend && pytest -q`; full `cd frontend && npm run test:unit -- --run`; `python backend/scripts/gen_contracts.py` then `git status` clean; `ruff check .` clean.
- [ ] Whole-branch review in the main loop (per feedback memory: fable reviews branches).
- [ ] PR to `dev`, body `Closes #324`, <= 8 bullets, one verification line. State: B-05 reserve ledger inflation; G-07 Render restart semantics; F-10/F-09/C-11 deviations; Q-03 folded; Q-05 left to the owner. Owed after merge: `alembic upgrade head` live via direct 5432 (0026, 0027) + indisvalid checks; F-10 `SET LOCAL` live smoke; B-05 burst repro; G-11 floor calibration on real documents; G-07 next-deploy check.
