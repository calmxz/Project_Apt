# QA audit re-triage — 2026-09-19
Re-verification of all 107 findings from the 2026-08-06 QA audit (`bug-tracker.csv`, git history `1d0f4aa`) against current code on `dev` @ 1d0f4aa. Each verdict cites file:line or commit. Method: six read-only verifier passes, one per category.
| Verdict | Count |
|---|---|
| ACCEPTED | 1 |
| FIXED | 30 |
| OBSOLETE | 1 |
| PARTIAL | 13 |
| STILL | 62 |

Open (STILL OPEN + PARTIAL) by severity: Critical 3, High 9, Medium 44, Low 19

## Open items, severity order

| ID | Severity | Verdict | Remaining work | Evidence |
|---|---|---|---|---|
| F-01 | Critical | PARTIAL | Add `pool_timeout` (~5s) to `_build_engine_kwargs` so overload sheds instead of hanging 30s; the worker/instance half is accepted per RUNBOOK. | `backend/entrypoint.sh:4` still plain `exec uvicorn` (no `--workers`); `render.yaml:7` still `plan: free`, no `numInstances`. Sizing is env-tunable and the single-instance ceiling is a recorded, deliberate choice: `backend/config.py:42-43` (`db_pool_size`/`db_max_overflow`, "B-10" note), wired at `backend/db/database.py:37-38`; `docs/deploy/RUNBOOK.md:110` records W-05 ("computed ceiling is 10 concurrent DB-holding requests on one uvicorn worker") and `RUNBOOK.md:136-140` states "keep the API at one instance and one uvicorn worker". No `pool_timeout` exists anywhere in the repo (grep: 0 hits). |
| F-02 | Critical | PARTIAL | Split `run()` so the blob load, extraction and chunking happen with no session held (load the Document row, `db.close()`/commit, re-open for the embed loop). | Fixed half: `backend/services/ingestion_service.py:141` `db.commit()  # release the connection before the network call`, per-`EMBED_BATCH` embed -> insert -> meter -> commit (`:127-171`). Commit `0822e0c` (PR #218). Remaining: `run()` opens the session at `ingestion_service.py:176` (`db.get(Document, ...)`) and the connection stays checked out through `_load_blob` (`:193`), `_extract` (`:194`), `chunking.chunk_text` (`:200`) and `_existing_chunk_indexes` (`:130`) until the first in-loop commit. With `ingest_in_process=True` (`config.py:48`, `backend/main.py:21-35`) that is the web pool. |
| F-03 | Critical | PARTIAL | Gate PDF/PPTX on page count at upload (or stream chunking page-by-page) so the flat token/page lists cannot be built for an oversized document; consider lowering `max_chunks` from 5000. | Fixed: vectors are no longer retained twice - each batch's vectors and raw response are discarded after its own commit (`backend/services/ingestion_service.py:127-171`, docstring `:29-35`); insert is a single bulk `insert().values()` with `ON CONFLICT DO NOTHING`, not per-chunk ORM objects (`backend/services/pgvector_store.py:53-68`). Hard chunk cap exists: `settings.max_chunks = 5000` (`backend/config.py:63`), enforced at upload time with 413 for plaintext (`backend/routes/upload.py:144-154`) and post-chunk in ingestion for everything else (`ingestion_service.py:206-210`). Commits `fcd360c` (PR #216) + `0822e0c` (PR #218). Remaining: `backend/lib/chunking.py:24-30` still materialises one flat token list plus a parallel page-of-token list for the whole document, and for `.pdf`/`.pptx` that happens *before* the cap fires. |
| C-01 | High | STILL OPEN | Replace the strict call at sessions.py:358 with `profile_service.profile_from_row(row)`. | `backend/routes/sessions.py:358` still calls `TopicProfile.model_validate_json(row.topic_profile_json)` inside `_to_match`; the tolerant parser (`profile_service.profile_from_row`, `backend/services/profile_service.py:153`, whose docstring at :113-126 documents exactly this hazard) is used by `chat.py:237` / `review.py:60` but not here. `TopicProfile` is still `extra="forbid"` (`backend/contracts/models.py:30-32`). No `exception_handler` exists, so the ValidationError still exits as a bare 500. |
| E-01 | High | STILL OPEN | Keep the form mounted; render start failures into a local `startError` ref instead of gating on `store.error`. | `frontend/src/views/HomeView.vue:6-10` — `v-if="store.error && !store.sessions.length"` still short-circuits the `<template v-else>` (lines 10-55) that wraps the topic input and Start button; `store.createSession` writes `store.error` via `_setError` (`frontend/src/stores/session.js:226,152-155`) and only `listSessions` clears it (`session.js:165`). Zero-session account + failed create still wipes the whole start block. |
| E-09 | High | STILL OPEN | Distinguish a failed hydrate from a successful "not complete" and do not force-route on failure; add a sign-out affordance to OnboardingView. | `frontend/src/stores/user.js:93-97` — `hydrateFromServer` still swallows the error in a bare catch and sets `hydrated = true` in `finally`, with no failure flag. `frontend/src/router/index.js:173-180` still redirects every route to `onboarding` on `!user.onboardingComplete`. `OnboardingView.vue` (whole file, 1-110) has no sign-out, back link or skip, and the only `signOut` call site is now `frontend/src/components/sidebar/Sidebar.vue:285`, which onboarding does not render. |
| F-05 | High | STILL OPEN | Materialise the centroid on `sessions` at ingestion time, or at minimum wrap in `run_in_threadpool` + cache per session id. | `backend/services/retrieval_service.py:111-122` still aggregates every chunk vector for the session (`func.avg(ChunkEmbedding.embedding)` over `session_id`); called at `:212` from `semantic_fallback_required`, which is awaited directly on the event loop at `backend/routes/chat.py:254`. No `sessions.chunk_centroid` column exists (grep `chunk_centroid`: 0 hits), no `run_in_threadpool`, no per-session cache. The `func.avg` (Postgres-side) form is not a remediation - it was introduced 2026-07-12 by `799bb08` (PR #113), before the audit. |
| F-06 | High | STILL OPEN | Delete `GET /api/sessions` (no production caller) or add the same `limit`/`offset` bounds, then update `docs/api/openapi.yaml` and regenerate contracts. | `backend/routes/sessions.py:206-216` `list_sessions` still selects every session for the user with no `limit`/`offset`, then `_enrich_list_items` (`:81-99`) enriches all of them. The sibling `/sessions/library` still caps at `Query(20, ge=1, le=100)` (`:289-290`). Production callers have moved off it: `frontend/src/stores/session.js:161-176` `listSessions()` now calls `getSessionLibrary`; `sessionsApi.listSessions` (`frontend/src/services/sessionsApi.js:22`) is referenced only from `frontend/src/__tests__/apiWrappers.test.js:47`. |
| F-07 | High | STILL OPEN | Push the latest-event-per-concept into SQL (window function), add a 90-day `created_at` floor, apply limit/offset in SQL, add `ix_learning_events_created_at`. | `backend/routes/review.py:26-37` still selects every non-diagnostic `LearningEvent` joined to sessions for the user with no `.limit()` and no `created_at` floor; pagination is still a Python slice at `:77` (`due[offset : offset + limit]`) and `total=len(due)` at `:79`. Only the per-session profile N+1 was fixed (batched `IN` fetch at `:50-58`). |
| F-08 | High | STILL OPEN | Replace the row scan with aggregate SQL (`count(*)`, `count(*) FILTER`, `max(coalesce(...))`) plus a `jsonb_array_elements` lateral, or maintain a denormalised per-user aggregate row. | `backend/services/profile_service.py:572-576` still scans every `SessionModel` row for the user; `:587-609` parses each row's full `topic_profile_json` via `_parse_profile`. A second unbounded pass follows at `:624-651` / `_learning_insights` over all `session_ids`. |
| F-11 | High | STILL OPEN | Convert the handlers to `def`, or wrap each synchronous DB segment in `starlette.concurrency.run_in_threadpool`. | `backend/routes/chat.py:118` `_prepare_turn` and `:324` `chat_stream` are `async def` performing synchronous `db.execute(...)` / `db.commit()` (`chat.py:139, 173, 221, 289, 297`); `backend/routes/sessions.py:128` `create_session`, `:457` `end_session`, `:669` `complete_check` likewise. Grep for `run_in_threadpool` across `backend/routes`: 0 hits. Engine and session are still sync (`backend/db/database.py:48-51`). |
| G-05 | High | PARTIAL | Redact the gap name at `profile_service.py:469-471` (hash/omit), or drop the root handler to WARNING and promote that call site deliberately | Logging half FIXED: `backend/lib/logging_config.py` (dictConfig, root handler at `settings.log_level`, formatter with timestamp/level/logger/request_id); called at `backend/main.py:16`; `RequestIdMiddleware` at `backend/lib/request_id.py` wired `backend/main.py:65`, `X-Request-Id` exposed `main.py:62`; `LOG_LEVEL` documented `.env.example:64-66`, `config.py:50`. Commit 27aa595 (PR #217). Not done: the PII caveat the fix called out — `backend/services/profile_service.py:468-473` still logs the literal gap name at INFO (`"focus_clear session=%s gap=%s reason=%s"`) and `config.py:50` defaults the root level to INFO, so prod now emits study-content PII that did not appear before |
| B-05 | Medium | STILL OPEN | Fold the gate into the write — have `record_cost` return the pre-increment total and reject when already over cap (or lock the ledger row off the streaming path). | Repro still succeeds. The gate is still an unlocked read: `backend/routes/chat.py:137-143` does a plain `SELECT` (`cost_meter.spend_subquery`) then `check_cap_from_spend`, with no row lock and no serialisable isolation. `cost_meter.record_cost` (`backend/services/cost_meter.py:80-103`) still returns the post-increment total and never rejects, so the check was not folded into the write. Burst is now capped at `BURST_LIMIT_PER_MINUTE=20` (`render.yaml:20-21`), which is exactly the finding's 20-request repro, so it does not close the window. |
| C-03 | Medium | STILL OPEN | Add an ASGI body-size middleware rejecting oversized non-multipart bodies (Content-Length pre-gate plus running byte count). | `backend/main.py:56-70` adds only `CORSMiddleware` and `RequestIdMiddleware` (`backend/lib/request_id.py`, request-id only — no body accounting). The only size guard in the codebase is upload-specific (`backend/routes/upload.py:80-91` content-length pre-gate + `_read_bounded` at :49-63). `ChatRequest.message` is still `constr(max_length=4000)` with no pre-parse size gate (`backend/contracts/models.py:331`), so a 10 MB JSON body is still fully buffered before the 422. |
| C-07 | Medium | STILL OPEN | Add the `sess.ended_at is not None -> 409 session_ended` check after the ownership check at upload.py:105, and document 409 for /api/upload in openapi.yaml. | `backend/routes/upload.py:103-105` checks ownership only (`sess is None or sess.user_id != user_id` -> 404) and then proceeds straight to the cost gate (:110) and rate-limit increment (:125). No `ended_at` check anywhere in the file, unlike `chat.py:185-186`, `sessions.py` check routes. No test references `session_ended` for upload (`backend/tests/` hits are chat/check routes only). |
| C-08 | Medium | STILL OPEN | Add a `content_sha256` column + unique index on (session_id, content_sha256) and return the existing row's 202 payload on conflict. | `backend/db/models.py:134-153` — `Document.__table_args__` now exists but contains only `Index("ix_documents_status_id", ...)` (added by migration `0023_worker_queue.py`, commit `0822e0c`); there is no content hash column and no unique constraint on (session_id, filename) or (session_id, sha). `backend/routes/upload.py:171` still creates `Document(session_id=..., filename=..., status="pending")` with no idempotency key. `services/velocity_limit.py` is a burst limiter, not a dedupe. |
| C-09 | Medium | STILL OPEN | None`. Code: `backend/routes/sessions.py:187` (`topic=req.topic.strip()`) and :583 (`row.topic = req.topic.strip()`) still have no post-strip emptiness guard. | Add `minLength: 1` to both topic schemas, regenerate contracts, and add a post-strip 422 guard in `create_session` and `update_session`. | Spec: `docs/api/openapi.yaml` has `minLength` only at :875, :1064, :1555, :1559, :1567 — none on the session `topic` schemas. Contracts: `backend/contracts/models.py:341` `topic: constr(max_length=200)` and :351 `topic: constr(max_length=200) \ |
| C-10 | Medium | STILL OPEN | Add `minLength: 1` in openapi.yaml, regenerate contracts, and a `if not req.message.strip(): raise HTTPException(422, ...)` as the first statement of `_prepare_turn`. | `backend/contracts/models.py:331` — `message: constr(max_length=4000)`, no `min_length`; openapi.yaml carries no `minLength` for it. `_prepare_turn` (`backend/routes/chat.py:137-210`) inspects cost caps, session, ended_at and the rate limiter but never `req.message`; the message is appended at :235 and persisted at :288/:295 regardless of content. |
| C-11 | Medium | STILL OPEN | Store an enumerated code on `documents.error`, keep `str(e)` in the log line only. | `backend/services/ingestion_service.py:263` — `doc.error = str(e)[:1000]` in the generic `except Exception` handler (the module docstring at :38 still describes this as the contract). Served verbatim on both surfaces the finding named: `backend/routes/upload.py:237` (`UploadStatus(..., error=doc.error)`) and `backend/routes/sessions.py:550` (`DocumentStatus(..., error=d.error)`). Logging is still env-gated (`exc_info=settings.env != "prod"`, :253) while the persisted message is not. |
| C-12 | Medium | PARTIAL | Commit the migration-lock checklist into the repo (e.g. `docs/reference.md` or the migration docs) so it survives a fresh clone; apply `SET lock_timeout = '5s'` on the next hot-table migration. | What landed in git: `backend/db/alembic/versions/0023_worker_queue.py:56-70` (commit `0822e0c`, PR #218) — the only migration added since the audit — builds the chunk_embeddings unique index with `CREATE UNIQUE INDEX CONCURRENTLY` inside `op.get_context().autocommit_block()`, then adopts it via `ADD CONSTRAINT ... UNIQUE USING INDEX`, exactly the recommended pattern. The three flagged migrations (0017/0019/0021) were correctly left applied as-is, per the finding's own "forward-guidance, not a rollback"; C-12 was scheduled in planning commit `d74041a`. What did NOT land durably: the checklist text lives only in `.claude/agents/migration-reviewer.md:29-40` ("Required patterns (C-12)") and `.claude/skills/project-conventions/SKILL.md:38`, and `.claude/` is gitignored (`.gitignore:18`; `git ls-files --error-unmatch` fails on both) — so the guidance is machine-local, not repo-durable. Also, 0023 touches hot tables (`chunk_embeddings`, `documents`) with no `SET lock_timeout`, and the NOT VALID/VALIDATE two-step has never been exercised. |
| C-13 | Medium | STILL OPEN | Document the four codes under their paths and add the CI reachability check. | All four undocumented codes remain, and no CI check was added. PATCH /api/sessions/{session_id} documents 200/400/404/401/503 (`docs/api/openapi.yaml:312-326`) while the handler raises 409 duplicate_topic (`backend/routes/sessions.py:579-582`). POST /api/sessions documents 201/400/409/401/503 (`openapi.yaml:104-122`) while the handler raises 422 for `declared_level` + `seed_mode=resume` (`sessions.py:143-147`). POST /api/upload documents 202/400/413/415/429/507/401/503 (`openapi.yaml:559-583`) with no 404, while `upload.py:105` raises 404. PATCH /api/me documents 400 (`openapi.yaml:273-274`) while `backend/routes/me.py:52-57` raises 422 "empty patch". |
| D-07 | Medium | PARTIAL | Same defect recurs at two new sites: `components/chat/MessageList.vue:68` `aria-label` on a `<p>` (role `paragraph`, name-prohibited) and `views/ProfileView.vue:233` `aria-label` on a `<span>` (role `generic`); replace with visually-hidden text. | All three cited sites are clean: `views/SettingsView.vue:8-11` is now a `<nav role="tablist" aria-label>` (role accepts a name); `components/chat/CheckQuestion.vue` has no `aria-label` on a `<p>`; `components/chat/MarkdownContent.vue` has no `aria-label` on a `<kbd>`. |
| D-09 | Medium | STILL OPEN | Gate the live region on `nearCharLimit` (>=90%) / at the limit, or drop `aria-live` and expose the limit via `aria-describedby`. | `components/chat/Composer.vue:157-159` — `<span v-if="modelValue.length" class="composer-count" aria-live="polite">{{ modelValue.length.toLocaleString() }} / ...</span>`. The text changes on every input event, so a screen reader announces a number per keystroke. No threshold gating (`nearCharLimit` at `:191` only drives colour via `:447-450`). |
| D-11 | Medium | STILL OPEN | Track `error` and `loading` separately from emptiness; add a distinct error branch with Retry and a skeleton while loading. | `views/ReviewView.vue:118-123` — `catch { queue.value = { items: [], total: 0 } } finally { loaded.value = true }`, comment "The review page must never block; show the empty state on failure" (introduced 5a74238, 2026-07-24, i.e. before the audit). `:56-59` then renders "Nothing due right now." `:108-110` mounts with `{ silent: true }`, so the toast is suppressed; there is no `error` ref and no loading branch, so the page is blank until the request settles. |
| D-12 | Medium | STILL OPEN | Use `width: min(24rem, calc(100vw - 2rem))` (or add `max-width` to `.crux-dialog` in `dialogs.css`) and verify at 320px. | `components/GapPickerDialog.vue:6` `:style="{ width: '24rem' }"` unchanged. No `max-width` anywhere for `.crux-dialog` in `assets/dialogs.css`, and PrimeVue's own `.p-dialog` base rule (`node_modules/@primeuix/styles/dist/dialog/index.mjs`) sets only `max-height: 90%` — no width cap. Overflows a 320px viewport. |
| D-13 | Medium | PARTIAL | Still missing: no `overflow-wrap`/`word-break` on message text (`UserBubble.vue:70-76` `.content`, `MarkdownContent.vue:169-178` `.md-rendered`), so a long unbroken URL overflows the bubble; and `MarkdownContent.vue:281-287` `table` has no `overflow-x: auto` wrapper, so a wide table still pushes the layout. | Landed: `min-width: 0` on the flex children (`components/chat/UserBubble.vue:47,67`, `components/chat/MessageList.vue:127`, `components/chat/AssistantBubble.vue:100,145`) and `overflow-x: auto` on code fences (`components/chat/MarkdownContent.vue:219-233`). |
| D-15 | Medium | STILL OPEN | Target a per-route stable element (router-view wrapper or per-view `<main>`/`h1`) and assert the node was found. | `router/index.js:185-192` still does `document.getElementById('main-content')?.focus()`. `#main-content` exists only inside `App.vue:115-120`, which is behind `v-if="showShell"`; chrome-less routes render through the `v-else` `RouterView` at `App.vue:131-135`, and those views' roots (`components/auth/AuthCover.vue:2`, `views/LegalView.vue:2`, `views/NotFoundView.vue:2`) carry no id. The optional chain swallows the miss silently. |
| D-16 | Medium | PARTIAL | Only the active panel is in the DOM (`:33-43` renders one `.panel`), so the three unselected tabs' `aria-controls` still dangle. Either render all four panels (hidden) or drop `aria-controls` on unselected tabs. | `views/SettingsView.vue:20` `:aria-controls="\`panel-${t.slug}\`"` and `:36` `:id="\`panel-${tab}\`"` now match for the selected tab, and `:37` adds the reciprocal `aria-labelledby`. |
| D-17 | Medium | PARTIAL | Two sidebar controls the finding named are still short and carry neither utility: the drawer-footer Settings link and Sign out button (`components/sidebar/Sidebar.vue:647-651`, `:676-681`) are `.sb-icon` at 2.25rem = 36px (`:1006-1011`) or 28px tall as a row (`:1022-1024`); and `components/sidebar/SidebarSessionRow.vue:265-271` `.sb-row-button` is `min-height: var(--line-pitch)` = 28px. Add `hit-44`/`coarse-2x` to those three. | Shared utilities landed in `assets/base.css:492-503` (`.hit-44` + coarse-pointer `::after { inset: -8px }`, i.e. 28px box -> 44px hit area) and `:505-515` (`.coarse-2x`, `min-height: 2 x --line-pitch` = 56px) — the finding's own recommended approach. Applied at `components/chat/Composer.vue:15,76,108` (the shrink-on-mobile rule is gone), `components/sidebar/Sidebar.vue:323,355`, `components/sidebar/SidebarRowMenu.vue:86`, `components/sidebar/SidebarMobileTopStrip.vue:13,35,41`, `views/SettingsView.vue:23`, plus `CueColumn`, `ProfileView`, `HomeView`, `SessionsLibraryView`. Check-question options get `min-height: 3.5rem` on coarse pointers (`CheckQuestion.vue:359-364`). Enforced by `__tests__/hitTargets.test.js`. |
| D-18 | Medium | STILL OPEN | Add `height: 100dvh` after `:713` (keep `100vh` as the fallback line), matching SessionView. | `components/sidebar/Sidebar.vue:713` `.sidebar { height: 100vh; }` — the only height rule in the file; `.sidebar--drawer` (`:752-765`) does not override it, and `100dvh` appears nowhere in Sidebar.vue. `views/SessionView.vue:1039-1040` still shows the correct `100vh` -> `100dvh` pair. The footer rail holding Settings and Sign out is the last flex child (`:646-704`). Mitigation: the "only exit" argument no longer holds — Settings is also in `components/sidebar/SidebarMobileTopStrip.vue:39-42`, outside the drawer, and Sign out now lives in the sidebar footer rather than behind Settings. |
| D-20 | Medium | STILL OPEN | Raise the resting control boundary (a dedicated token at >=3:1 on `--card`) for the composer and any other input edges. | `components/chat/Composer.vue:275` `border: 1px solid var(--card-edge)` is still the composer's only resting boundary. `--card-edge` = `#cfd3da` on `--card` `#ffffff` = 1.50:1 (light) and `#2e323a` on `#22252b` = 1.19:1 (dark); the card-vs-desk edge is 1.23:1. Both fail 1.4.11. Focus is fine (`:281-283` switches the border to `--ink-learner`, 7.08:1). No rationale recorded in `docs/decisions.md` or in the file's comments. |
| E-02 | Medium | STILL OPEN | Mirror line 61 in the `:93-105` catch: `throw e instanceof TypeError ? new ApiError(0, {detail: e.message}, path) : e`. | `frontend/src/services/chatStreamService.js:105` — body-phase catch still does bare `throw e`; the header-phase normalization is present one block up at `:61`. `friendlyError` returns `err.message` for a non-ApiError (`frontend/src/lib/errors.js:45`), so a mid-stream `TypeError` still renders "Failed to fetch". |
| E-03 | Medium | STILL OPEN | Before `_clearStreamState()` in the generic catch, push a non-empty `streamingMessage.content` into `messages` with status `error`, as `handleAbortError` does. | `frontend/src/stores/session.js:906-909` — the generic transport catch calls `_clearStreamState()` (nulls `streamingMessage`, `session.js:689-693`) then `_setError(e)`, discarding the partial. The SSE-event sibling still retains it (`handleAbortError`, `session.js:775-784`). An idle-timeout rejects as `ApiError(0,'stream timed out')` (`chatStreamService.js:94`), not `AbortError`, so it lands in this arm. |
| E-04 | Medium | STILL OPEN | | data.code`. `_applyCapError` (`session.js:146-150`) only sets cap state and does not suppress the banner, so the cost-cap event still renders `daily_cost_cap_reached` next to the CapBanners alert. `lib/errorCodes.js` + `lib/errors.js` CODE_COPY exist but are only consulted for `err.body.detail.code` (`errors.js:28-29`), never for SSE `data.code`. | Route SSE `data.code` through a code-to-copy map and suppress the banner when `_applyCapError` claimed the event. | `frontend/src/stores/session.js:858` (and identical at `:642`) — still `error.value = data.message |
| E-06 | Medium | PARTIAL | Add Retry to the empty-state error branch (`:279`); decide whether the library load should be `{silent:true}` so toast + page are not duplicated. Dead `store.libraryError` (`session.js:91,109`) is still rendered nowhere. | Raw envelope is gone: `frontend/src/views/SessionsLibraryView.vue:106` now uses `friendlyError(e)` (commit `1d0f4aa`). Retry exists only inside the sentinel block, which is `v-if="items.length"` (`SessionsLibraryView.vue:336-349`); a first-load failure renders the bare paragraph at `:279-281` with no control. `store.fetchLibrary` still calls `getSessionLibrary` without `{silent:true}` (`session.js:103-113`), so the errorBus toast still fires alongside the page copy. |
| E-07 | Medium | STILL OPEN | Track a failed flag and retry with backoff while `status` is null or pending; render a "references unavailable - retry" row. | `frontend/src/components/chat/ReferenceStatusBanner.vue:162-167` — the catch keeps the last known state and the retry is still scheduled only `if (... status.value === 'pending')`; after a first-poll failure `status` is still `null` (`:128`), so no timer is set and the banner's `v-if="status"` (`:2`) hides the whole block including the per-file delete buttons (`:87-111`). |
| E-08 | Medium | STILL OPEN | Add a `writing` ref that disables mutating controls and chains each write onto the previous ETag; add confirm or undo to `removeItem`/`removeSubtopic`. | `frontend/src/views/ProfileView.vue:348-364` — `_applyWrite` still has no in-flight guard, and every caller reads `data.value.etag` at call time (`:370,374,378,383,388`). The 412 arm still sets `conflict` and silently reloads (`:357-359`), rendering "Profile changed elsewhere" (`:37-39`). Chip remove calls `removeItem` directly with no confirm (`:176-179`), as does `removeSubtopic` (`:124-127`), while document delete does confirm (`ReferenceStatusBanner.vue:179-180`). |
| E-10 | Medium | STILL OPEN | Add a Retry button bound to `load()`, and/or `onActivated` refetch when the previous attempt errored. | `frontend/src/components/settings/UsageTab.vue:26-34` loads only in `onMounted` with error copy "Usage data is unavailable right now." (`:9-11`) and no control; `frontend/src/components/settings/ProfileTab.vue:110-122` is `onMounted(load)` with a bare `friendlyError` paragraph (`:41`). `frontend/src/views/SettingsView.vue:40-42` still wraps the panel in `<KeepAlive>`, so tab switching re-activates rather than re-mounts. |
| E-12 | Medium | STILL OPEN | Route End through `useConfirm` with the file-delete styling contract. | `frontend/src/components/sidebar/SidebarSessionRow.vue:53-69` — `onEnd` still calls `store.endSession` directly with only a `busy` guard; no `useConfirm` anywhere in `frontend/src/components/sidebar/`. The menu item is still a danger-styled entry stacked with Rename/Pin (`SidebarRowMenu.vue:190`), while document delete does use `confirm.require` (`ReferenceStatusBanner.vue:179-180`). |
| E-13 | Medium | STILL OPEN | \| <fallback>`, and `needsConfirm` is still a regex on SDK prose (`LoginView.vue:170`). Commit `1d0f4aa` added a comment at `LoginView.vue:165-168` (repeated at the other three views) rejecting *one specific* alternative — routing AuthErrors through `friendlyError()`, which carries an HTTP status and would clobber the copy and break the not-confirmed detection. It does not address the finding's actual recommendation (a code/status-keyed auth-copy helper) and says nothing about the regex coupling, so this is a constraint on the fix, not an acceptance of the defect. | Map `AuthError` code/status to owned copy in a dedicated helper (not `friendlyError`, per the `1d0f4aa` constraint), and drive `needsConfirm` off the error code rather than prose. | `frontend/src/views/LoginView.vue:168-170`, `RegisterView.vue:159`, `ForgotPasswordView.vue:102`, `ResetPasswordView.vue:137`, `AccountTab.vue:203` all still do `error.value = e?.message \ |
| F-09 | Medium | PARTIAL | Add `ix_chat_messages_session_id_desc (session_id, id DESC)` and `ix_learning_events_created_at (created_at, id)`, built CONCURRENTLY. | Landed: ingestion-poll shape covered by `0022_documents_session_idx.py:23` (`ix_documents_session_id`) and `0023_worker_queue.py:36-37` (`ix_documents_status_id`); usage-summary shape covered by the `daily_cost_ledger` PK `(user_id, date_utc)` (`backend/db/models.py:179-180`) and `ix_llm_call_log_user` (`models.py:194`) against `backend/services/usage_service.py:29-51`. Missing: no index on `chat_messages (session_id, id)` anywhere in `backend/db/alembic/versions/` - only `ix_chat_messages_session_created (session_id, created_at)` (`0008_session_perf_indexes.py:25-29`), while `backend/routes/sessions.py:229-230` orders by `ChatMessage.id.desc()`; and no `learning_events (created_at)` index - only `ix_learning_events_session` (`0008:30-33`). |
| F-10 | Medium | STILL OPEN | Denormalise readiness onto `chunk_embeddings`, add `SET LOCAL hnsw.ef_search` on the retrieval transaction, plan partitioning. | `backend/services/pgvector_store.py:88-100` still combines the HNSW `ORDER BY distance LIMIT k` with a join filter on the other table (`Document.status == "ready"`, `:96`). No `chunk_embeddings.doc_ready` column exists (grep `doc_ready`: only an unrelated subquery label at `backend/routes/chat.py:179`). No `hnsw.ef_search` is ever set on the query transaction (grep: only the prose note in `0017_hnsw_chunk_embeddings.py:11`). |
| F-12 | Medium | STILL OPEN | Delete `pollUploadStatus` and drive the chip from the banner poll; add exponential backoff with a cap. | Both pollers survived the September rewrite. `frontend/src/views/SessionView.vue:881-908` `pollUploadStatus` polls `getUploadStatus` at a fixed 1s interval, now for up to 90 attempts (was 30); `frontend/src/components/chat/ReferenceStatusBanner.vue:155-166` independently re-polls every 2000 ms. No backoff on either. The attempt ceiling with a "still processing" message does exist (`SessionView.vue:909-912`). |
| F-13 | Medium | STILL OPEN | Insert `db.commit()` immediately before `semantic_fallback_required` so the connection is released across both awaited embedding round-trips. | the transaction that spans the awaits opens at `backend/routes/chat.py:221` (the history select; `rate_limit.check_and_increment` committed just before, at `:204`) and there is no `db.commit()` before the awaited embedding calls at `:254` (`semantic_fallback_required`) and `:261-263` (`prefetch_for_prompt`); the first commit after them is the user-message commit at `:297`. `embedding_timeout_s` is still 15.0 (`backend/config.py:73`). The `cost_holder` boundary-survival mechanism the fix depends on is already in place (`chat.py:219` `embed_cost_holder`, `:285-287`). |
| F-15 | Medium | STILL OPEN | Cache the rendered HTML of the stable prefix and render only the new slice, falling back to a full render when the anchor is invalidated. | `frontend/src/components/chat/MarkdownContent.vue:15-25`: the `parts` computed still calls `renderMarkdown(safe)` on the whole accumulated safe prefix on every reactive update; `renderMarkdown` (`frontend/src/lib/markdownRenderer.js:208`) does a full markdown-it parse plus a DOMPurify pass with no memoisation, and the fence rule re-highlights every block (`:170-193`). (`cb1c64c`, 2026-09-14, made KaTeX/hljs lazy but did not touch the per-frame re-render.) `splitSafePrefixIncremental` incrementalises only the scan, not the render. |
| F-16 | Medium | STILL OPEN | Virtualise the transcript or cap retained history (~200 messages) and drop the oldest page on prepend. | `frontend/src/components/chat/MessageList.vue:49-58` still renders every visible message inside a `TransitionGroup` with no windowing; `frontend/src/stores/session.js:310` prepends older pages (`messages.value = [...page.items, ...messages.value]`) with no trimming or retained-history cap. |
| F-17 | Medium | STILL OPEN | Add the `/assets/(.*)` -> `public, max-age=31536000, immutable` header block to `vercel.json`. | `frontend/vercel.json:9-19` still defines only the four security headers - no `Cache-Control` entry for `/assets/`. `frontend/nginx.conf` still sets its own 30d expiry for the same path, so the two targets still disagree. |
| F-18 | Medium | STILL OPEN | Return/honour HTTP `ETag`/`If-None-Match` on the hot GETs, add a short TTL GET cache, and wrap `request` in bounded exponential-backoff retry for network errors and 502/503/504 only. | `frontend/src/services/apiClient.js:111-166` `request()` sends no `If-None-Match` and no `Cache-Control`, stores nothing, and the only retry is still the single 401 refresh at `:148-151`. A 30s `AbortSignal.timeout` was added (`:11, :131-132`) but no backoff for network errors or 502/503/504. Backend sends no HTTP `ETag` either - the `etag` field on profile responses (`backend/routes/profile.py:63`) is a body value used for `If-Match` optimistic concurrency, not response caching. |
| F-19 | Medium | STILL OPEN | Guard the refresh with a `threading.Lock` + double-checked read and construct `PyJWKClient(url, cache_jwk_set=True, lifespan=3600)` explicitly; better, refresh on a background timer. | Nothing from the recommended fix landed: `backend/services/auth.py:34-46` `_get_jwks_client` still has no `threading.Lock` and no double-checked read (grep `threading` under `backend/services/`: 0 hits), and `PyJWKClient(...)` is still constructed with no explicit `cache_jwk_set`/`lifespan` (`auth.py:45, :66`). The `_JWKS_TTL_SECONDS = 60 * 60` constant (`auth.py:23`) and the boot warm-up (`auth.py:49-72` `validate_jwks_startup`, called from `backend/main.py:43`) both predate the audit - `auth.py` was last touched 2026-07-17 (`2cbce77`, PR #121), before the 2026-08-06 audit, so they are the state the finding was written against, not a remediation. |
| G-04 | Medium | STILL OPEN | Return a coarse `tool_failed` code at `tools.py:112` (log `str(e)`), keeping ValidationError as the safe passthrough | `backend/agent/tools.py:110-112` — blanket `except Exception` still returns `ToolResult(..., error=str(e))`; `backend/agent/tutor.py:418-421` still yields that string in the `tool_call_done` SSE event. Source exceptions unchanged: `backend/services/profile_service.py:192` and `:206` raise `ValueError` interpolating the session id. Only the retrieval path was hardened (`retrieval_service.py:85-91` returns coarse `retrieval_failed`) |
| G-07 | Medium | STILL OPEN | Add `/ready` running `db.execute(select(1))` under a short timeout, return 503 on failure, repoint `render.yaml:10` (and the test) | `backend/routes/health.py:8-19` returns a constant `HealthResponse(status="ok")` touching no dependency; `render.yaml:10` still `healthCheckPath: /health`; no `/ready` route exists anywhere in `backend/routes/`; `backend/tests/test_deploy_config.py:29` asserts the probe path stays `/health`. No recorded acceptance in `docs/decisions.md` |
| G-10 | Medium | STILL OPEN | Add `timeout-minutes: 15` plus an `if: failure()` notify step (or a healthcheck ping on success) | `.github/workflows/backup.yml` — the `backup` job (lines 11-17) still has no `timeout-minutes`, and the workflow has no `if: failure()` step, no webhook/issue notification and no dead-man's-switch ping; last touched by dependency/path fixes only (`git log -- .github/workflows/backup.yml`) |
| G-11 | Medium | STILL OPEN | Apply a similarity floor on `h.score` before building citations, or intersect against model-declared doc_ids | `backend/agent/tutor.py:437-445` builds a `Citation` for every chunk in `result.data["chunks"]` with no score test; `backend/routes/chat.py:305-315` does the same for the prefetch path; `retrieval_service.py:93-108` and `:168-180` return all top-k hits with `score` carried but never gated (`retrieval_fallback_threshold` is used only for the semantic gate at `:224`) |
| G-12 | Medium | STILL OPEN | Add the 12 vars to `.env.example`, pin the 4 in `render.yaml`, add the drift test | `.env.example` (66 lines) still omits ENV, DB_POOL_SIZE, DB_MAX_OVERFLOW, LLM_TEMPERATURE, SUMMARY_TEMPERATURE, RETRIEVAL_FALLBACK_THRESHOLD, LLM_TIMEOUT_S, SUMMARY_TIMEOUT_S, EMBEDDING_TIMEOUT_S, MAX_PROFILE_LIST, DEBUG_TIMING, SUPABASE_JWKS_URL_OVERRIDE (all present in `backend/config.py:19-88`); only the new `LOG_LEVEL` was added (`.env.example:64-66`). `render.yaml:11-45` still has no MODEL, EMBEDDING_MODEL, DB_POOL_SIZE or DB_MAX_OVERFLOW. `backend/tests/test_deploy_config.py` has no Settings-vs-template drift assertion. Drift has since grown: INGEST_IN_PROCESS, BURST_LIMIT_PER_MINUTE, LLM_RETRY_ATTEMPTS, LLM_RETRY_BASE_DELAY_S are also absent from the template |
| Q-03 | Medium | STILL OPEN | flake8\|mypy\|pylint` across toml/cfg/ini/yml/yaml/txt/py/json/md/sh hits only prose in `README.md:598` ("ruff-compatible Python"). The backend CI job (`.github/workflows/ci.yml`) has no lint step. | Add `ruff` to backend dev extras plus a `ruff check .` step in the backend CI job; clear the first-run backlog in a dedicated PR. | No Python linter or type checker exists. `backend/pyproject.toml:36-44` dev extras are still pytest, pytest-asyncio, httpx, pytest-cov, datamodel-code-generator, openapi-spec-validator, pyyaml only. Repo-wide grep for `ruff\ |
| Q-05 | Medium | PARTIAL | Enable branch protection on `dev` and `main` requiring the Backend, Frontend and Security checks (run `docs/deploy/enable-branch-protection.sh`). | Trigger half is fixed: `.github/workflows/ci.yml:4-5` is `push: branches: [main, dev]` and `.github/workflows/e2e.yml:7-8` likewise, so direct pushes to `dev` now run CI and E2E. Commit `97ae80b` (PR #215). Branch-protection half is not done and is deliberately deferred: `docs/decisions.md` records `docs/deploy/enable-branch-protection.sh` as "W-07 deferred, not done". |
| A-01 | Low | STILL OPEN | denylist\|jti\|revoke` over `backend/` returns zero matches; `backend/services/auth.py` still validates signature/exp only. The recommended documentation target `docs/security/SECURITY_REVIEW.md` was deleted (see `docs/decisions.md:2026-09-19` table) and no doc records the residual window — grep for `revoke\|revocation\|residual\|stateless\|logout` over `docs/` returns zero matches. | Record the stateless-token tradeoff and the Supabase access-token TTL as the worst-case residual window (now needs a home in `docs/decisions.md` or `docs/reference.md`, since `SECURITY_REVIEW.md` is gone). | No revocation primitive exists: repo-wide grep for `token_valid_after\ |
| B-08 | Low | STILL OPEN | Pick one source of truth, document the deployed `0.8`/`1.00` (or align them), and widen the soft-to-hard spacing so the warning is actionable. | Divergence unchanged: `render.yaml:22-25` pins `0.8`/`1.00` while `backend/config.py:61-62` defaults `2.00`/`3.00`, `.env.example:44-45` says `2.00`/`3.00`, `docker-compose.prod.yml:39-40` falls back to `2.00`/`3.00`, and `README.md:234` and `docs/auth/supabase-setup.md:82-83` both document `2.00`/`3.00`. `cost_meter.py:172` still derives urgent as `hard_cap * 0.9`, so the deployed tiers remain 0.80/0.90/1.00. No doc records the deployed values. |
| C-14 | Low | STILL OPEN | Register a global handler in main.py returning the coded `ErrorResponse` shape with the request id, and map service-layer `ValueError` to 409/422. | Repo-wide grep for `exception_handler` returns zero hits outside vendored packages; `backend/main.py:56-70` registers only CORS + RequestId middleware and no handlers. Unmapped raisers are unchanged. (Partial credit only for the correlation-id prerequisite: `X-Request-Id` is now emitted by `backend/lib/request_id.py:22-30`.) |
| C-15 | Low | STILL OPEN | Add CheckConstraints for `documents.status` and `chat_messages.role` plus a NOT VALID/VALIDATE migration. | `backend/db/models.py:145` — `status: Mapped[str] = mapped_column(String, default="pending")`, no CheckConstraint; :88 — `role: Mapped[str] = mapped_column(String, nullable=False)`, no CheckConstraint. `Document.__table_args__` (:136-138) holds only an index. The selective pattern is still visible at :79-82 where `chat_messages.status` does get a CHECK. Note the finding's specific repro value is now stale: `processing` is a legitimate worker state and is accepted by `UploadStatus.status` (`backend/contracts/models.py:538`), `DocumentStatus` (:552) and `SessionIngestionStatus` (:566), so that value no longer 500s — any other out-of-band value still does, and the DB-level gap is unchanged. `docs/deploy/RESTORE.md:81` only records that the hazard "did not fire" on the drill dataset — an observation, not a recorded acceptance. |
| C-16 | Low | STILL OPEN | Migration adding `server_default=now()` then `SET NOT NULL` after backfill. | `backend/db/alembic/versions/0001_phase7_baseline.py:27,36,47,63,73` — all five `created_at` columns still `nullable=True`; no later migration alters them (0002-0023 contain no created_at ALTER). `backend/db/models.py:21,62,92,125,151,173` supply a Python-side `default=_utcnow` only, no `server_default`. `0023_worker_queue.py:42-44` explicitly reasons around "created_at (nullable=True per 0002)", confirming the state. |
| C-17 | Low | STILL OPEN | None = None` with no `Query(..., max_length=...)`; :300 — `base.where(SessionModel.topic.ilike(f"%{q}%"))` with no `%`/`_`/backslash escaping and no `escape=` argument. Spec unchanged: `docs/api/openapi.yaml:151-154` declares `q` as `{ type: [string, "null"], default: null }` with no maxLength, in contrast to the capped `topic` param on /sessions/lookup. | Cap `q` at 200 in route + spec and escape LIKE metacharacters with an explicit `escape=`. | `backend/routes/sessions.py:287` — `q: str \ |
| C-18 | Low | STILL OPEN | Append `ChatMessage.id.desc()` to both order_by clauses. | `backend/routes/chat.py:224` — `.order_by(ChatMessage.created_at.desc())` with no secondary key; `backend/routes/sessions.py:659` — same. The contrast cited in the finding also still holds: `services/session_enrichment.py:78` uses `(created_at.desc(), id.desc())` and `sessions.py:307` uses `(created_at.desc(), id.desc())`. |
| D-21 | Low | PARTIAL | `views/SessionView.vue` still has no `h1` before a topic resolves: the only `h1` in the session branch is inside `components/chat/SessionHeader.vue:22-23`, gated on `v-if="topic"`, and `SessionView.vue:452-455` `headerTopic` returns `''` until `current`/`knownRow` loads (`SessionView.vue:6`'s `h1` is the 404 branch only). Add a visually-hidden fallback `h1`. | Sidebar half fixed: `components/sidebar/Sidebar.vue:537` is now `<h2 class="sb-section-label label">`, no `h3` remains. |
| D-22 | Low | STILL OPEN | Give the link a resting affordance (underline or learner-ink colour), or promote it to a labelled control. | `components/chat/SessionHeader.vue:85-88` `.session-topic-link { text-decoration: none; cursor: pointer; }` — at rest the link is styled identically to `.session-topic` (`:71-83`, same font/colour/size). The only hints remain hover (`:90-95`) and the `title` attribute (`:28`). `:26` `/session/${sessionId}/profile` is still the sole link to that route anywhere in `src/**/*.vue`. Mitigation: `components/chat/CueColumn.vue` now surfaces focus/gaps/mastered/level inline in the session, so the header link is no longer the only way to see that data — just the only way to reach the full profile page. |
| D-25 | Low | STILL OPEN | Give Skip its own grid column, or delete the composer Skip now that CheckQuestion owns it — resolve alongside E-20. | Unchanged. `components/chat/Composer.vue:396-400` puts `.composer-send`/`.composer-stop` on `grid-column: 3` and `:452-453` puts `.composer-skip` on `grid-column: 3` too; both render together when `locked` is true and the stream is idle (`:73-74`, `:131-132`). Still latent: `stores/session.js:495` `const checkLocked = computed(() => false)`, passed at `views/SessionView.vue:177`, so Skip never renders. (CheckQuestion has since grown its own Skip at `CheckQuestion.vue:119-128`.) |
| E-15 | Low | STILL OPEN | Drop the terminal `uploadStatus` write and let ReferenceStatusBanner own steady-state ingestion status. | `frontend/src/views/SessionView.vue:908-912` — the poll loop still writes a terminal `kind: 'pending'` "is still processing" caption that nothing clears (window widened to 90 iterations at `:882`, from unrelated ingestion-worker work). `ReferenceStatusBanner` keeps its own 2s poll and flips to "N references ready" (`ReferenceStatusBanner.vue:149-151,165-167`), and both are stacked at `SessionView.vue:152-154`. |
| E-17 | Low | STILL OPEN | Expose `store.checkAnswering` to the card and disable/pend the options on first click. | `frontend/src/components/chat/CheckQuestion.vue:70-77` — options are still gated only on `answered` (derived from `item.status`, `:17`), which flips only after the POST returns. The `busy` prop (`:10`) is bound to `store.streamState !== 'idle'` (`SessionView.vue:89,104`), not to `checkAnswering`, and drives only Skip/Next/Done (`:124,136,147`). `frontend/src/stores/session.js:524` is still a silent `if (checkAnswering.value) return`. |
| E-18 | Low | STILL OPEN | `watch(() => props.id, load)` plus a load-sequence discriminator. | `frontend/src/views/ProfileView.vue:400` is still `onMounted(load)` with no `watch(() => props.id, ...)`; `topicLabel` still resolves from the store (`:327-330`). SessionView solves the same problem at `SessionView.vue:645-656`. Reachability caveat from the original finding still applies. |
| E-19 | Low | STILL OPEN | `begin(quickTopic.value).catch(() => {})` in `startQuick`. | `frontend/src/views/HomeView.vue:97-99` — `startQuick` still calls `begin(quickTopic.value)` with no await and no `.catch()`; `useStartFlow.js:15-38` `begin` has try/finally only and `_create` rethrows non-409 (`useStartFlow.js:87`), while `store.createSession` rethrows via `_setError` (`session.js:226,152-155`). Sibling call sites still carry their deliberate empty catches (`SessionsLibraryView.vue:27`, `SidebarSessionRow.vue:65`). |
| E-20 | Low | STILL OPEN | Delete the `locked` prop and its three branches, or make `checkLocked` reflect a real condition. | `frontend/src/stores/session.js:495` — `checkLocked` is still `computed(() => false)` (comment and computed both date to `45b242f`, pre-audit, so this is not a post-audit acceptance). `frontend/src/views/SessionView.vue:176` binds it to Composer, whose three dependent branches are all still live decoys: attach lock (`Composer.vue:17`), Skip button (`:131-139`), placeholder (`:197-199`), plus the unreachable `@skip` wiring (`SessionView.vue:180`). |
| F-20 | Low | STILL OPEN | Add the re-entry guard, hoist `handler` to module scope, export a `dispose()`. | `frontend/src/composables/useTheme.js:41-57` `init()` still adds the `matchMedia` change listener with no removal path, no `dispose()` export and no `if (mediaQuery) return` re-entry guard; `handler` is still a local closure. Impact remains latent (single call site at bootstrap). |
| F-21 | Low | STILL OPEN | | \`m-${i}\`"`), and `session.js:310` still prepends. | Generate a local `message_id` (e.g. `crypto.randomUUID()`) on the optimistic push and reconcile it in `finalizeMessage`; update the `:891` undefined-check accordingly. | `frontend/src/stores/session.js:807` still pushes the optimistic row as `{ role: 'user', content: trimmed }` with no `message_id` (and `:891` relies on `message_id === undefined` to pop it on failure); `frontend/src/components/chat/MessageList.vue:50` still falls back to an index key (`:key="m.message_id |
| G-13 | Low | STILL OPEN | Extract `_stream_one_iteration`, `_meter_iteration`, `_dispatch_tool_calls` | `backend/agent/tutor.py:136-629` — `run_streaming` is still a single function of ~494 lines; the only module-level helpers are `_persist_assistant_message:31`, `_persist_partial_on_abort:54`, `_summarize:78`, `_record_partial_cost:90`, `_chunk_text:121`. None of the three proposed seams were extracted; the deliberate double-count comment survives at `:591-600` |
| G-14 | Low | STILL OPEN | Extract the cross-session insights half of `profile_service.py` into its own module | `wc -l`: `backend/services/profile_service.py` = 695 (grown from 667), `backend/agent/tutor.py` = 630 (from 612). No `services/profile_insights.py` exists |

## Closed items

| ID | Severity | Verdict | Evidence |
|---|---|---|---|
| B-01 | Critical | FIXED | `backend/routes/upload.py:107-119` calls `cost_meter.assert_within_caps(db, user_id)` before `rate_limit.check_and_increment` and returns the 429 cap envelope. Ingestion also re-gates per batch: `backend/services/ingestion_service.py:162-168`, `:233-244` (cap-reached stops ingestion and marks the doc). Plaintext chunk ceiling added at `upload.py:144-154` (`CHUNK_LIMIT_EXCEEDED`, `settings.max_chunks`). Commit `fcd360c` (PR #216). |
| B-02 | Critical | FIXED | Ingestion no longer runs on the request threadpool: repo-wide grep for `BackgroundTasks\ |
| B-04 | High | FIXED | Fleet-wide ceiling exists: `backend/services/cost_meter.py:197-204` (`global_spend`, `SUM(cost_usd) WHERE date_utc = today`) and `:207-215` (`assert_within_caps` raises `GLOBAL_COST_CAP_REACHED`). Enforced on chat at `backend/routes/chat.py:156-166` and on upload at `routes/upload.py:111`. Env var `GLOBAL_DAILY_COST_CAP_USD` in `backend/config.py:64` and `.env.example:47`. Commit `fcd360c` (PR #216). Note: the var defaults to `None` and is absent from `render.yaml`, so the gate is a no-op until armed; arming it is an explicit deploy step at `docs/deploy/RUNBOOK.md:51`. Mechanism landed; arming is a RUNBOOK gate. |
| B-07 | Medium | FIXED | `backend/services/velocity_limit.py` implements a per-user sliding 60 s window; wired as a dependency on both paid endpoints: `backend/routes/upload.py:70` and `backend/routes/chat.py:323` (`Depends(velocity_limit.enforce_velocity)`). Configured in prod via `render.yaml:20-21` (`BURST_LIMIT_PER_MINUTE=20`). Introduced in commit `4e0466d` ("audit remediation 2026-09-02"). Accepted as in-process rather than shared-store, with the single-instance rationale recorded in the module docstring (`velocity_limit.py:1-12`). |
| C-02 | High | FIXED | `frontend/nginx.conf:39` — `client_max_body_size 25m;` inside `location /api/`, matching `MAX_UPLOAD_BYTES` at `backend/routes/upload.py:29`. Commit `3cd1c07` ("fix: screen-reader blockers + nginx upload size (D-01..D-04, C-02)"). The suggested >1 MB compose smoke was not added, but the limit drift itself is closed. |
| D-01 | High | FIXED | `components/chat/CheckQuestion.vue:57-67` sr-only `role="status" aria-live="polite" aria-atomic="true"` wrapper present-but-empty before grading; `:76-77` `aria-disabled` + no-op click instead of `disabled`; `:31-39` nextTick focus move to Next/Done. Test `__tests__/checkQuestion.test.js:190` ("CheckQuestion accessibility (D-01)"). Commit 3cd1c07 (#219). |
| D-02 | High | FIXED | All five sites now carry `role="alert"`: `views/LoginView.vue:66`, `views/RegisterView.vue:55`, `views/ForgotPasswordView.vue:20`, `views/ResetPasswordView.vue:46`, `components/settings/AccountTab.vue:46` and `:91`. Commit 3cd1c07 (#219). |
| D-03 | High | FIXED | `components/sidebar/Sidebar.vue:313` `:inert="(!isDesktop && mode !== 'drawer-open') \ |
| D-04 | High | FIXED | `components/start/StartTopicIntercept.vue:26-32` `role="status" aria-live="polite" aria-label="Existing session found"`; `:17-22` `onMounted` + nextTick focus onto `primaryBtn` (the Open/Continue action). Commit 3cd1c07 (#219). |
| D-05 | High | FIXED | `assets/base.css:188` (and mirrored at `:250`) dark `--color-text-faint: #a3a39c`. Computed against the dark surfaces: background `#15171b` 7.07:1, surface/card `#22252b` 6.05:1, surface-soft `#1b1d22` 6.65:1, surface-raised `#1f2126` 6.35:1 — all clear 4.5:1. Light `#63635e` is 6.04:1 on `#ffffff`, 4.92:1 on `#e6e8ec`. Palette rewritten in the Sept redesign; `__tests__/tokenContrast.test.js:46` guards the background case. |
| D-08 | Medium | FIXED | `views/SessionsLibraryView.vue:209` root is now `<section aria-labelledby="library-title">`; the file contains no `<main>`. `App.vue:120` `<main id="main-content">` is the single landmark on `/sessions`. Sept redesign. `__tests__/landmarks.test.js` pins the chrome-less views to one `<main>` each. |
| D-10 | Medium | FIXED | `assets/base.css:113` `--color-accent-text: #1d4fc4`, `:115` `--color-accent-soft: #e0e7f4` — now an opaque hex, so no alpha compositing. Computed 5.70:1. Dark pair at `:193`/`:197` keeps a translucent soft; composited over `#22252b` it resolves to ~`#313949`, giving ~5.4:1 (dark already passed at audit time). Sept redesign palette. |
| D-14 | Medium | FIXED | `components/chat/Composer.vue:497-501` — the `max-width: 599px` rule now hides only `.composer-hint` (the Enter/Shift+Enter string); `.composer-count` (`:157`) stays visible at every width. Truncation is no longer silent either: `:66` `:maxlength="MAX_DRAFT_LEN"` (4000, `:187`) hard-stops the textarea, and `:191`/`:447-450` turn the counter red and bold from 90% of the limit. |
| D-19 | Medium | FIXED | Correctness now has a non-colour channel: distinct tick vs cross SVG marks at `components/chat/CheckQuestion.vue:81-103` plus a text verdict "Correct" / "Not quite" at `:108-110` and an sr-only announcement at `:64-66`. The signal is no longer a border — `.check-card` keeps the neutral `--card-edge` (`:166`) and nothing applies `--signal-success`/`--signal-warning` as a border. The marks use `--ink-marker` (`base.css:31`/`:166`): 4.39:1 on the white card in light, 5.47:1 in dark, both clearing 3:1. Sept redesign. |
| D-23 | Low | FIXED | `components/settings/AppearanceTab.vue:63-67` `MODES = [light 'Lamp on', dark 'Lamp off', auto 'Match system']` rendered as a three-option radio group (`:6-54`), wired to `setTheme` which accepts `auto` (`composables/useTheme.js:4,58-63`). `system` is reachable again after any manual pick. Sept redesign. |
| D-24 | Low | FIXED | No component-scoped `[data-theme='dark']` rules remain anywhere in `src/**/*.{vue,css}` (only `base.css:162` and `:227`, plus a comment at `AppearanceTab.vue:117`). `assets/base.css:226-285` mirrors the entire dark token set under `@media (prefers-color-scheme: dark)`, and `__tests__/tokenContrast.test.js:130-156` is a drift guard asserting the attribute-dark and media-dark blocks declare identical token sets. |
| E-05 | High | FIXED | `frontend/src/stores/session.js:894-897` rethrows `StreamAbortedError('auth_expired', e)`; `frontend/src/views/SessionView.vue:707-719` stashes the text to `sessionStorage` key `crux:draft:<id>`, and `restoreStashedDraft` (`SessionView.vue:739-748`) rehydrates it on mount/id change (`:655`). Class added in `frontend/src/lib/errors.js:49-58`. Commit `09af5da`. |
| E-11 | Medium | FIXED | `frontend/src/stores/session.js:899-905` — the `session_ended` 409 arm now sets the banner, clears stream state and rethrows `StreamAbortedError('session_ended', e)`; `frontend/src/views/SessionView.vue:707-719` restores `draft.value = text`. Commit `09af5da`. |
| E-14 | Low | FIXED | `frontend/src/views/SessionView.vue:729-737` — `retryLastMessage` now only falls back to `lastSentText` when the composer is empty (`:734`), with the E-14 rationale in-comment. Commit `09af5da`. |
| E-16 | Low | OBSOLETE | The Danger zone and the "Retake onboarding" link no longer exist in `frontend/src/components/settings/AccountTab.vue` (file is Account name + Security password change only, 1-125); removed across `8c750d3` (settings rail) and `6c22a47`. `resetOnboarding` is still dead (`frontend/src/stores/user.js:113-118`, referenced only by `frontend/src/__tests__/userStore.test.js`) and the router still honours `?retake=1` (`router/index.js:181-183`), but the miscalibrated-danger concern does not carry over. |
| F-04 | High | FIXED | Ingestion is now a durable DB-backed queue, not an in-process `BackgroundTask`: `backend/db/alembic/versions/0023_worker_queue.py` (documents.claimed_at, `ix_documents_status_id`, unique `(document_id, chunk_index)`); `backend/worker.py:34-51` `claim_next` with `FOR UPDATE SKIP LOCKED`; `worker.py:53-67` `recover_stuck` re-queues stale `processing` rows back to `pending` (stall window `worker.py:24` `STALE_PROCESSING_MINUTES = 30`, swept every ~60s via `worker.py:31` `RECOVER_EVERY_ITERATIONS`) (no longer marks them failed - `reap_stale_pending` is gone, grep: 0 hits); `ingestion_service.py:130` `_existing_chunk_indexes` makes re-runs idempotent and never re-pays; per-batch commits keep purchased embeddings durable. `backend/main.py:38-52` drains the queue in a daemon thread with a bounded shutdown join. Commit `0822e0c` (PR #218). The out-of-process worker service is deliberately deferred with recorded rationale at `docs/deploy/RUNBOOK.md:25-39`. |
| F-14 | High | FIXED | `frontend/src/lib/markdownRenderer.js:64-97` now dynamic-imports `@mdit/plugin-katex`, `katex/dist/katex.min.css` and `highlight.js`, so neither is in the statically analysable entry graph (comment `:5-10` records the P1 rationale). Verified against the finding's own check: `frontend/dist/index.html` carries only `index-*.js`, three small modulepreloads and `index-*.css` - no `markdownRenderer-*.js` preload and no `markdownRenderer-*.css` stylesheet link. Commit `cb1c64c` (2026-09-14, "perf(markdown): lazy-load KaTeX and highlight.js"). |
| G-01 | High | FIXED | `backend/agent/prompts.py:279-289` wraps `last_session_summary` and `rolling_summary` via `wrap_untrusted(_SUMMARY_TAG, ...)`; generic tag-neutralizing fence at `backend/agent/excerpt.py:20-30`; matching "UNTRUSTED SUMMARIES" rule block at `backend/agent/prompts.py:206-210`; injection-resistant instruction added to `SUMMARY_SYSTEM` (`backend/services/summary_service.py:24-30`) and `ROLLING_SYSTEM` (`:151-158`). Commit f1f9a93 (PR #241). Tests: `backend/tests/test_excerpt.py:49`, `backend/tests/test_prompts.py:352` |
| G-02 | Medium | FIXED | `_flatten()` at `backend/services/summary_service.py:33-37` collapses CR/LF; applied on every transcript join: `:42`, `:69`, `:167`, `:212`. Commit f1f9a93 (PR #241). Tests: `backend/tests/test_summary_service.py:377`, `backend/tests/test_rolling_summary.py:153` |
| G-03 | Medium | FIXED | `backend/agent/prompts.py:328-338` builds `review_gaps_label` with `json.dumps(review_gaps_target)` on both the retention and plain branches. Commit f1f9a93 (PR #241). Test: `backend/tests/test_prompts.py:352` |
| G-06 | High | FIXED | All four agent-loop handlers now carry correlation fields: `backend/agent/tutor.py:70-74`, `:558-562`, `:578-582`, `:618-622` (`session_id=%s user_id=%s`), rendered by the G-05 config. Commit 27aa595 (PR #217) |
| G-09 | Medium | FIXED | `docs/deploy/RESTORE.md:21-45` now contains the "Proven restore log": run 2026-08-07 against `pgvector/pgvector:pg17`, PASS, mechanical RTO 2 min 29 s, real `pg_restore` output and dump listing pasted. Commit f7946f5. (RPO is still 24 h by design — `backup.yml:5` cron unchanged; that was the optional half of the recommendation) |
| G-15 | Low | FIXED | `backend/agent/tools.py:91-95` now carries an explicit comment naming `ctx.session_id` injection as the authoritative control and the per-service checks as downstream guards; the guard was mirrored into `check_question_service.register` (`backend/services/check_question_service.py:129-132`), alongside the existing `profile_service.py:336-340` and `retrieval_service.py:60-64`. The mixed state the finding objected to is gone. Judgment call: the literal "one-line comment at each guard" was not added — the explanatory comment lives only on the tools.py side |
| Q-01 | High | FIXED | `backend/tests/conftest.py:5` sets `os.environ["CRUX_SKIP_DOTENV"] = "1"` before `config` is imported; `backend/config.py:9-13` returns `env_file=None` under that flag, so no repo-root `.env` (and no live DATABASE_URL / GEMINI_API_KEY / SUPABASE_SECRET_KEY / R2 creds) enters a pytest process. Guarded by a regression test at `backend/tests/test_hermetic_settings.py:9`. CI sets the same flag (`.github/workflows/ci.yml:61`). Commit `97ae80b` (PR #215). Note: `tests/test_usage_route.py:22` still asserts the literal `2.0` rather than `settings.llm_soft_cap_usd`, which is now correct-by-construction since the suite always sees code defaults. |
| Q-02 | Medium | FIXED | `frontend/package.json:14-16` — `lint` runs `lint:oxlint` = `oxlint .` and `lint:eslint` = `eslint . --cache`, neither with `--fix`. Auto-fix moved to separate `fix:*` scripts (`:17-19`). CI's `npm run lint` therefore now fails on fixable violations. Commit `97ae80b` (PR #215). |
| Q-04 | Medium | FIXED | `backend/pyproject.toml:53` addopts now reads `--cov=services --cov=lib --cov=worker --cov=routes --cov=agent --cov=db --cov-report=term-missing --cov-fail-under=75`. `routes/`, `agent/` and `db/` are measured (plus `worker`). Commit `09af5da` (PR #220). The floor stayed at 75 against the widened scope, which is the stricter reading of the recommendation. |
| Q-06 | Low | ACCEPTED | Settings are unchanged — `.github/workflows/ci.yml:170-171` still `bandit -r backend -lll -iii`, `:184-199` still trivy `severity: CRITICAL` with `ignore-unfixed: true` on both images — but the finding's own recommendation was "no change required before launch", and the accuracy concern it raised is addressed: the thresholds are recorded explicitly in `docs/security/CI_INVENTORY.md:16` ("high severity + high confidence only — suppresses noise") and `:33-34` ("CRITICAL only (with ignore-unfixed)"). |
